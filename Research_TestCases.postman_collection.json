{
	"info": {
		"_postman_id": "bed9be22-ef24-4c09-a40a-f1ecf03d22e1",
		"name": "Research_TC_Git",
		"schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json",
		"_exporter_id": "15071380"
	},
	"item": [
		{
			"name": "Freightos - shippingCalculator [Box Shipment] - JSON",
			"item": [
				{
					"name": "Box Shipment - Orgn=CN & Dst=US",
					"event": [
						{
							"listen": "test",
							"script": {
								"exec": [
									"pm.test(\"Status code is 200\", function () {\r",
									"    pm.response.to.have.status(200);\r",
									"});\r",
									"\r",
									"/*pm.test(\"Response time is below 3000ms\", function () {\r",
									"    pm.expect(pm.response.responseTime).to.be.below(3000);\r",
									"});*/\r",
									"\r",
									"pm.test(\"Response object exists\", function () {\r",
									"    const jsonData = pm.response.json();\r",
									"    pm.expect(jsonData).to.have.property(\"response\");\r",
									"});\r",
									"\r",
									"pm.test(\"estimatedFreightRates is present\", function () {\r",
									"    const jsonData = pm.response.json();\r",
									"    pm.expect(jsonData.response).to.have.property(\"estimatedFreightRates\");\r",
									"});\r",
									"\r",
									"pm.test(\"Mode array is not empty\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode;\r",
									"    pm.expect(modes.length).to.be.greaterThan(0);\r",
									"});\r",
									"\r",
									"pm.test(\"Expected shipping modes are available\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode\r",
									"        .map(m => m.mode);\r",
									"\r",
									"    pm.expect(modes).to.include.members([\"air\", \"express\", \"LCL\"]);\r",
									"});\r",
									"\r",
									"pm.test(\"Min price is less than max price for each mode\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode;\r",
									"\r",
									"    modes.forEach(m => {\r",
									"        pm.expect(m.price.min.moneyAmount.amount)\r",
									"            .to.be.below(m.price.max.moneyAmount.amount);\r",
									"    });\r",
									"});\r",
									"\r",
									"pm.test(\"Currency is USD for all modes\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode;\r",
									"\r",
									"    modes.forEach(m => {\r",
									"        pm.expect(m.price.min.moneyAmount.currency).to.eql(\"USD\");\r",
									"        pm.expect(m.price.max.moneyAmount.currency).to.eql(\"USD\");\r",
									"    });\r",
									"});\r",
									"\r",
									"pm.test(\"Transit time min is less than max\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode;\r",
									"\r",
									"    modes.forEach(m => {\r",
									"        pm.expect(m.transitTimes.min)\r",
									"            .to.be.below(m.transitTimes.max);\r",
									"    });\r",
									"});\r",
									"\r",
									"pm.test(\"Transit time unit is days\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode;\r",
									"\r",
									"    modes.forEach(m => {\r",
									"        pm.expect(m.transitTimes.unit).to.eql(\"days\");\r",
									"    });\r",
									"});\r",
									"\r",
									"pm.test(\"Number of quotes matches mode count\", function () {\r",
									"    const data = pm.response.json().response.estimatedFreightRates;\r",
									"    pm.expect(data.numQuotes).to.eql(data.mode.length);\r",
									"});"
								],
								"type": "text/javascript",
								"packages": {}
							}
						}
					],
					"request": {
						"method": "GET",
						"header": [],
						"url": {
							"raw": "{{URL}}loadtype=boxes&weight=200&width=50&length=50&height=50&origin=Shanghai,China&quantity=2&destination=NewYork,NY",
							"host": [
								"{{URL}}loadtype=boxes&weight=200&width=50&length=50&height=50&origin=Shanghai,China&quantity=2&destination=NewYork,NY"
							]
						}
					},
					"response": []
				},
				{
					"name": "Box Shipment - Orgn=US & Dst=CN",
					"event": [
						{
							"listen": "test",
							"script": {
								"exec": [
									"pm.test(\"Status code is 200\", function () {\r",
									"    pm.response.to.have.status(200);\r",
									"});\r",
									"\r",
									"/*pm.test(\"Response time is below 3000ms\", function () {\r",
									"    pm.expect(pm.response.responseTime).to.be.below(3000);\r",
									"});*/\r",
									"\r",
									"pm.test(\"Response object exists\", function () {\r",
									"    const jsonData = pm.response.json();\r",
									"    pm.expect(jsonData).to.have.property(\"response\");\r",
									"});\r",
									"\r",
									"pm.test(\"estimatedFreightRates is present\", function () {\r",
									"    const jsonData = pm.response.json();\r",
									"    pm.expect(jsonData.response).to.have.property(\"estimatedFreightRates\");\r",
									"});\r",
									"\r",
									"pm.test(\"Mode array is not empty\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode;\r",
									"    pm.expect(modes.length).to.be.greaterThan(0);\r",
									"});\r",
									"\r",
									"pm.test(\"Expected shipping modes are available\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode\r",
									"        .map(m => m.mode);\r",
									"\r",
									"    pm.expect(modes).to.include.members([\"air\", \"express\", \"LCL\"]);\r",
									"});\r",
									"\r",
									"pm.test(\"Currency is USD for all modes\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode;\r",
									"\r",
									"    modes.forEach(m => {\r",
									"        pm.expect(m.price.min.moneyAmount.currency).to.eql(\"USD\");\r",
									"        pm.expect(m.price.max.moneyAmount.currency).to.eql(\"USD\");\r",
									"    });\r",
									"});\r",
									"\r",
									"pm.test(\"Transit time min is less than max\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode;\r",
									"\r",
									"    modes.forEach(m => {\r",
									"        pm.expect(m.transitTimes.min)\r",
									"            .to.be.below(m.transitTimes.max);\r",
									"    });\r",
									"});\r",
									"\r",
									"pm.test(\"Transit time unit is days\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode;\r",
									"\r",
									"    modes.forEach(m => {\r",
									"        pm.expect(m.transitTimes.unit).to.eql(\"days\");\r",
									"    });\r",
									"});\r",
									"\r",
									"pm.test(\"Number of quotes matches mode count\", function () {\r",
									"    const data = pm.response.json().response.estimatedFreightRates;\r",
									"    pm.expect(data.numQuotes).to.eql(data.mode.length);\r",
									"});"
								],
								"type": "text/javascript"
							}
						}
					],
					"request": {
						"method": "GET",
						"header": [],
						"url": {
							"raw": "{{URL}}loadtype=boxes&weight=200&width=50&length=50&height=50&origin=NewYork,NY&quantity=2&destination=Shanghai,China",
							"host": [
								"{{URL}}loadtype=boxes&weight=200&width=50&length=50&height=50&origin=NewYork,NY&quantity=2&destination=Shanghai,China"
							]
						}
					},
					"response": []
				},
				{
					"name": "Box Shipment - Orgn=MY & Dst=AU",
					"event": [
						{
							"listen": "test",
							"script": {
								"exec": [
									"pm.test(\"Status code is 200\", function () {\r",
									"    pm.response.to.have.status(200);\r",
									"});\r",
									"\r",
									"/*pm.test(\"Response time is below 3000ms\", function () {\r",
									"    pm.expect(pm.response.responseTime).to.be.below(3000);\r",
									"});*/\r",
									"\r",
									"pm.test(\"Response object exists\", function () {\r",
									"    const jsonData = pm.response.json();\r",
									"    pm.expect(jsonData).to.have.property(\"response\");\r",
									"});\r",
									"\r",
									"pm.test(\"estimatedFreightRates is present\", function () {\r",
									"    const jsonData = pm.response.json();\r",
									"    pm.expect(jsonData.response).to.have.property(\"estimatedFreightRates\");\r",
									"});\r",
									"\r",
									"pm.test(\"Mode array is not empty\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode;\r",
									"    pm.expect(modes.length).to.be.greaterThan(0);\r",
									"});\r",
									"\r",
									"/* 🔧 MODIFIED: Make expected modes flexible */\r",
									"pm.test(\"Expected shipping modes are valid\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode\r",
									"        .map(m => m.mode);\r",
									"\r",
									"    const allowedModes = [\"air\", \"express\", \"LCL\"];\r",
									"    modes.forEach(mode => {\r",
									"        pm.expect(allowedModes).to.include(mode);\r",
									"    });\r",
									"});\r",
									"\r",
									"/* 🔧 MODIFIED: Allow min to be equal to max */\r",
									"pm.test(\"Min price is less than or equal to max price for each mode\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode;\r",
									"\r",
									"    modes.forEach(m => {\r",
									"        pm.expect(m.price.min.moneyAmount.amount)\r",
									"            .to.be.at.most(m.price.max.moneyAmount.amount);\r",
									"    });\r",
									"});\r",
									"\r",
									"pm.test(\"Currency is USD for all modes\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode;\r",
									"\r",
									"    modes.forEach(m => {\r",
									"        pm.expect(m.price.min.moneyAmount.currency).to.eql(\"USD\");\r",
									"        pm.expect(m.price.max.moneyAmount.currency).to.eql(\"USD\");\r",
									"    });\r",
									"});\r",
									"\r",
									"pm.test(\"Transit time min is less than max\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode;\r",
									"\r",
									"    modes.forEach(m => {\r",
									"        pm.expect(m.transitTimes.min)\r",
									"            .to.be.below(m.transitTimes.max);\r",
									"    });\r",
									"});\r",
									"\r",
									"pm.test(\"Transit time unit is days\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode;\r",
									"\r",
									"    modes.forEach(m => {\r",
									"        pm.expect(m.transitTimes.unit).to.eql(\"days\");\r",
									"    });\r",
									"});\r",
									"\r",
									"pm.test(\"Number of quotes matches mode count\", function () {\r",
									"    const data = pm.response.json().response.estimatedFreightRates;\r",
									"    pm.expect(data.numQuotes).to.eql(data.mode.length);\r",
									"});\r",
									""
								],
								"type": "text/javascript",
								"packages": {}
							}
						}
					],
					"request": {
						"method": "GET",
						"header": [],
						"url": {
							"raw": "{{URL}}loadtype=boxes&weight=500&width=50&length=70&height=50&origin=KualaLumpur,Malaysia&quantity=5&destination=Sydney,Australia",
							"host": [
								"{{URL}}loadtype=boxes&weight=500&width=50&length=70&height=50&origin=KualaLumpur,Malaysia&quantity=5&destination=Sydney,Australia"
							]
						}
					},
					"response": []
				},
				{
					"name": "Box Shipment - Orgn=AU & Dst=MY",
					"event": [
						{
							"listen": "test",
							"script": {
								"exec": [
									"pm.test(\"Status code is 200\", function () {\r",
									"    pm.response.to.have.status(200);\r",
									"});\r",
									"\r",
									"/*pm.test(\"Response time is below 3000ms\", function () {\r",
									"    pm.expect(pm.response.responseTime).to.be.below(3000);\r",
									"});*/\r",
									"\r",
									"pm.test(\"Response object exists\", function () {\r",
									"    const jsonData = pm.response.json();\r",
									"    pm.expect(jsonData).to.have.property(\"response\");\r",
									"});\r",
									"\r",
									"pm.test(\"estimatedFreightRates is present\", function () {\r",
									"    const jsonData = pm.response.json();\r",
									"    pm.expect(jsonData.response).to.have.property(\"estimatedFreightRates\");\r",
									"});"
								],
								"type": "text/javascript"
							}
						}
					],
					"request": {
						"method": "GET",
						"header": [],
						"url": {
							"raw": "{{URL}}loadtype=boxes&weight=500&width=50&length=70&height=50&origin=Sydney,Australia&quantity=5&destination=KualaLumpur,Malaysia",
							"host": [
								"{{URL}}loadtype=boxes&weight=500&width=50&length=70&height=50&origin=Sydney,Australia&quantity=5&destination=KualaLumpur,Malaysia"
							]
						}
					},
					"response": []
				},
				{
					"name": "Box Shipment - Orgn=DE & Dst=CA",
					"event": [
						{
							"listen": "test",
							"script": {
								"exec": [
									"pm.test(\"Status code is 200\", function () {\r",
									"    pm.response.to.have.status(200);\r",
									"});\r",
									"\r",
									"/*pm.test(\"Response time is below 3000ms\", function () {\r",
									"    pm.expect(pm.response.responseTime).to.be.below(3000);\r",
									"});*/\r",
									"\r",
									"pm.test(\"Response object exists\", function () {\r",
									"    const jsonData = pm.response.json();\r",
									"    pm.expect(jsonData).to.have.property(\"response\");\r",
									"});\r",
									"\r",
									"pm.test(\"estimatedFreightRates is present\", function () {\r",
									"    const jsonData = pm.response.json();\r",
									"    pm.expect(jsonData.response).to.have.property(\"estimatedFreightRates\");\r",
									"});\r",
									"\r",
									"pm.test(\"Mode array is not empty\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode;\r",
									"    pm.expect(modes.length).to.be.greaterThan(0);\r",
									"});\r",
									"\r",
									"pm.test(\"Expected shipping modes are valid\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode\r",
									"        .map(m => m.mode);\r",
									"\r",
									"    const allowedModes = [\"air\", \"express\", \"LCL\"];\r",
									"    modes.forEach(mode => {\r",
									"        pm.expect(allowedModes).to.include(mode);\r",
									"    });\r",
									"});\r",
									"\r",
									"pm.test(\"Min price is less than or equal to max price for each mode\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode;\r",
									"\r",
									"    modes.forEach(m => {\r",
									"        pm.expect(m.price.min.moneyAmount.amount)\r",
									"            .to.be.at.most(m.price.max.moneyAmount.amount);\r",
									"    });\r",
									"});\r",
									"\r",
									"pm.test(\"Currency is USD for all modes\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode;\r",
									"\r",
									"    modes.forEach(m => {\r",
									"        pm.expect(m.price.min.moneyAmount.currency).to.eql(\"USD\");\r",
									"        pm.expect(m.price.max.moneyAmount.currency).to.eql(\"USD\");\r",
									"    });\r",
									"});\r",
									"\r",
									"pm.test(\"Transit time min is less than or equal to max\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode;\r",
									"\r",
									"    modes.forEach(m => {\r",
									"        pm.expect(m.transitTimes.min)\r",
									"            .to.be.at.most(m.transitTimes.max);\r",
									"    });\r",
									"});\r",
									"\r",
									"pm.test(\"Transit time unit is days\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode;\r",
									"\r",
									"    modes.forEach(m => {\r",
									"        pm.expect(m.transitTimes.unit).to.eql(\"days\");\r",
									"    });\r",
									"});\r",
									"\r",
									"pm.test(\"Number of quotes matches mode count\", function () {\r",
									"    const data = pm.response.json().response.estimatedFreightRates;\r",
									"    pm.expect(data.numQuotes).to.eql(data.mode.length);\r",
									"});"
								],
								"type": "text/javascript",
								"packages": {}
							}
						}
					],
					"request": {
						"method": "GET",
						"header": [],
						"url": {
							"raw": "{{URL}}loadtype=boxes&weight=300&width=55&length=60&height=40&origin=Hamburg,Germany&quantity=7&destination=Toronto,Canada",
							"host": [
								"{{URL}}loadtype=boxes&weight=300&width=55&length=60&height=40&origin=Hamburg,Germany&quantity=7&destination=Toronto,Canada"
							]
						}
					},
					"response": []
				},
				{
					"name": "Box Shipment - Orgn=CA & Dst=DE",
					"event": [
						{
							"listen": "test",
							"script": {
								"exec": [
									"pm.test(\"Status code is 200\", function () {\r",
									"    pm.response.to.have.status(200);\r",
									"});\r",
									"\r",
									"/*pm.test(\"Response time is below 3000ms\", function () {\r",
									"    pm.expect(pm.response.responseTime).to.be.below(3000);\r",
									"});*/\r",
									"\r",
									"pm.test(\"Response object exists\", function () {\r",
									"    const jsonData = pm.response.json();\r",
									"    pm.expect(jsonData).to.have.property(\"response\");\r",
									"});\r",
									"\r",
									"pm.test(\"estimatedFreightRates is present\", function () {\r",
									"    const jsonData = pm.response.json();\r",
									"    pm.expect(jsonData.response).to.have.property(\"estimatedFreightRates\");\r",
									"});\r",
									"\r",
									"pm.test(\"Mode array is not empty\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode;\r",
									"    pm.expect(modes.length).to.be.greaterThan(0);\r",
									"});\r",
									"\r",
									"pm.test(\"Expected shipping modes are valid\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode\r",
									"        .map(m => m.mode);\r",
									"\r",
									"    const allowedModes = [\"air\", \"express\", \"LCL\"];\r",
									"    modes.forEach(mode => {\r",
									"        pm.expect(allowedModes).to.include(mode);\r",
									"    });\r",
									"});\r",
									"\r",
									"pm.test(\"Min price is less than or equal to max price for each mode\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode;\r",
									"\r",
									"    modes.forEach(m => {\r",
									"        pm.expect(m.price.min.moneyAmount.amount)\r",
									"            .to.be.at.most(m.price.max.moneyAmount.amount);\r",
									"    });\r",
									"});\r",
									"\r",
									"pm.test(\"Currency is USD for all modes\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode;\r",
									"\r",
									"    modes.forEach(m => {\r",
									"        pm.expect(m.price.min.moneyAmount.currency).to.eql(\"USD\");\r",
									"        pm.expect(m.price.max.moneyAmount.currency).to.eql(\"USD\");\r",
									"    });\r",
									"});\r",
									"\r",
									"pm.test(\"Number of quotes matches mode count\", function () {\r",
									"    const data = pm.response.json().response.estimatedFreightRates;\r",
									"    pm.expect(data.numQuotes).to.eql(data.mode.length);\r",
									"});"
								],
								"type": "text/javascript"
							}
						}
					],
					"request": {
						"method": "GET",
						"header": [],
						"url": {
							"raw": "{{URL}}loadtype=boxes&weight=300&width=55&length=60&height=40&origin=Toronto,Canada&quantity=7&destination= Hamburg,Germany",
							"host": [
								"{{URL}}loadtype=boxes&weight=300&width=55&length=60&height=40&origin=Toronto,Canada&quantity=7&destination= Hamburg,Germany"
							]
						}
					},
					"response": []
				},
				{
					"name": "Box Shipment - Orgn=NL & Dst=AE",
					"event": [
						{
							"listen": "test",
							"script": {
								"exec": [
									"pm.test(\"Status code is 200\", function () {\r",
									"    pm.response.to.have.status(200);\r",
									"});\r",
									"\r",
									"/*pm.test(\"Response time is below 3000ms\", function () {\r",
									"    pm.expect(pm.response.responseTime).to.be.below(3000);\r",
									"});*/\r",
									"\r",
									"pm.test(\"Response object exists\", function () {\r",
									"    const jsonData = pm.response.json();\r",
									"    pm.expect(jsonData).to.have.property(\"response\");\r",
									"});\r",
									"\r",
									"pm.test(\"estimatedFreightRates is present\", function () {\r",
									"    const jsonData = pm.response.json();\r",
									"    pm.expect(jsonData.response).to.have.property(\"estimatedFreightRates\");\r",
									"});\r",
									"\r",
									"pm.test(\"Mode array is not empty\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode;\r",
									"    pm.expect(modes.length).to.be.greaterThan(0);\r",
									"});\r",
									"\r",
									"pm.test(\"Expected shipping modes are valid\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode\r",
									"        .map(m => m.mode);\r",
									"\r",
									"    const allowedModes = [\"air\", \"express\", \"LCL\"];\r",
									"    modes.forEach(mode => {\r",
									"        pm.expect(allowedModes).to.include(mode);\r",
									"    });\r",
									"});\r",
									"\r",
									"pm.test(\"Min price is less than or equal to max price for each mode\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode;\r",
									"\r",
									"    modes.forEach(m => {\r",
									"        pm.expect(m.price.min.moneyAmount.amount)\r",
									"            .to.be.at.most(m.price.max.moneyAmount.amount);\r",
									"    });\r",
									"});\r",
									"\r",
									"pm.test(\"Currency is USD for all modes\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode;\r",
									"\r",
									"    modes.forEach(m => {\r",
									"        pm.expect(m.price.min.moneyAmount.currency).to.eql(\"USD\");\r",
									"        pm.expect(m.price.max.moneyAmount.currency).to.eql(\"USD\");\r",
									"    });\r",
									"});\r",
									"\r",
									"pm.test(\"Transit time min is less than or equal to max\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode;\r",
									"\r",
									"    modes.forEach(m => {\r",
									"        pm.expect(m.transitTimes.min)\r",
									"            .to.be.at.most(m.transitTimes.max);\r",
									"    });\r",
									"});\r",
									"\r",
									"pm.test(\"Transit time unit is days\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode;\r",
									"\r",
									"    modes.forEach(m => {\r",
									"        pm.expect(m.transitTimes.unit).to.eql(\"days\");\r",
									"    });\r",
									"});\r",
									"\r",
									"pm.test(\"Number of quotes matches mode count\", function () {\r",
									"    const data = pm.response.json().response.estimatedFreightRates;\r",
									"    pm.expect(data.numQuotes).to.eql(data.mode.length);\r",
									"});\r",
									""
								],
								"type": "text/javascript",
								"packages": {}
							}
						}
					],
					"request": {
						"method": "GET",
						"header": [],
						"url": {
							"raw": "{{URL}}loadtype=boxes&weight=250&width=40&length=50&height=50&origin=Amsterdam,Netherlands&quantity=4&destination=Dubai,UAE",
							"host": [
								"{{URL}}loadtype=boxes&weight=250&width=40&length=50&height=50&origin=Amsterdam,Netherlands&quantity=4&destination=Dubai,UAE"
							]
						}
					},
					"response": []
				},
				{
					"name": "Box Shipment - Orgn=AE & Dst=NL",
					"event": [
						{
							"listen": "test",
							"script": {
								"exec": [
									"pm.test(\"Status code is 200\", function () {\r",
									"    pm.response.to.have.status(200);\r",
									"});\r",
									"\r",
									"/*pm.test(\"Response time is below 3000ms\", function () {\r",
									"    pm.expect(pm.response.responseTime).to.be.below(3000);\r",
									"});*/\r",
									"\r",
									"pm.test(\"Response object exists\", function () {\r",
									"    const jsonData = pm.response.json();\r",
									"    pm.expect(jsonData).to.have.property(\"response\");\r",
									"});\r",
									"\r",
									"pm.test(\"estimatedFreightRates is present\", function () {\r",
									"    const jsonData = pm.response.json();\r",
									"    pm.expect(jsonData.response).to.have.property(\"estimatedFreightRates\");\r",
									"});\r",
									"\r",
									"pm.test(\"Mode array is not empty\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode;\r",
									"    pm.expect(modes.length).to.be.greaterThan(0);\r",
									"});\r",
									"\r",
									"pm.test(\"Expected shipping modes are valid\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode\r",
									"        .map(m => m.mode);\r",
									"\r",
									"    const allowedModes = [\"air\", \"express\", \"LCL\"];\r",
									"    modes.forEach(mode => {\r",
									"        pm.expect(allowedModes).to.include(mode);\r",
									"    });\r",
									"});\r",
									"\r",
									"pm.test(\"Min price is less than or equal to max price for each mode\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode;\r",
									"\r",
									"    modes.forEach(m => {\r",
									"        pm.expect(m.price.min.moneyAmount.amount)\r",
									"            .to.be.at.most(m.price.max.moneyAmount.amount);\r",
									"    });\r",
									"});\r",
									"\r",
									"pm.test(\"Currency is USD for all modes\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode;\r",
									"\r",
									"    modes.forEach(m => {\r",
									"        pm.expect(m.price.min.moneyAmount.currency).to.eql(\"USD\");\r",
									"        pm.expect(m.price.max.moneyAmount.currency).to.eql(\"USD\");\r",
									"    });\r",
									"});\r",
									"\r",
									"pm.test(\"Transit time min is less than or equal to max\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode;\r",
									"\r",
									"    modes.forEach(m => {\r",
									"        pm.expect(m.transitTimes.min)\r",
									"            .to.be.at.most(m.transitTimes.max);\r",
									"    });\r",
									"});\r",
									"\r",
									"pm.test(\"Transit time unit is days\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode;\r",
									"\r",
									"    modes.forEach(m => {\r",
									"        pm.expect(m.transitTimes.unit).to.eql(\"days\");\r",
									"    });\r",
									"});\r",
									"\r",
									"pm.test(\"Number of quotes matches mode count\", function () {\r",
									"    const data = pm.response.json().response.estimatedFreightRates;\r",
									"    pm.expect(data.numQuotes).to.eql(data.mode.length);\r",
									"});\r",
									""
								],
								"type": "text/javascript"
							}
						}
					],
					"request": {
						"method": "GET",
						"header": [],
						"url": {
							"raw": "{{URL}}loadtype=boxes&weight=250&width=40&length=50&height=50&origin=Dubai,UAE&quantity=4&destination=Amsterdam,Netherlands",
							"host": [
								"{{URL}}loadtype=boxes&weight=250&width=40&length=50&height=50&origin=Dubai,UAE&quantity=4&destination=Amsterdam,Netherlands"
							]
						}
					},
					"response": []
				},
				{
					"name": "Box Shipment - Orgn=IN & Dst=DE",
					"event": [
						{
							"listen": "test",
							"script": {
								"exec": [
									"pm.test(\"Status code is 200\", function () {\r",
									"    pm.response.to.have.status(200);\r",
									"});\r",
									"\r",
									"/*pm.test(\"Response time is below 3000ms\", function () {\r",
									"    pm.expect(pm.response.responseTime).to.be.below(3000);\r",
									"});*/\r",
									"\r",
									"pm.test(\"Response object exists\", function () {\r",
									"    const jsonData = pm.response.json();\r",
									"    pm.expect(jsonData).to.have.property(\"response\");\r",
									"});\r",
									"\r",
									"pm.test(\"estimatedFreightRates is present\", function () {\r",
									"    const jsonData = pm.response.json();\r",
									"    pm.expect(jsonData.response).to.have.property(\"estimatedFreightRates\");\r",
									"});\r",
									"\r",
									"pm.test(\"Mode array is not empty\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode;\r",
									"    pm.expect(modes.length).to.be.greaterThan(0);\r",
									"});\r",
									"\r",
									"pm.test(\"Expected shipping modes are valid\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode\r",
									"        .map(m => m.mode);\r",
									"\r",
									"    const allowedModes = [\"air\", \"express\", \"LCL\"];\r",
									"    modes.forEach(mode => {\r",
									"        pm.expect(allowedModes).to.include(mode);\r",
									"    });\r",
									"});\r",
									"\r",
									"pm.test(\"Min price is less than or equal to max price for each mode\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode;\r",
									"\r",
									"    modes.forEach(m => {\r",
									"        pm.expect(m.price.min.moneyAmount.amount)\r",
									"            .to.be.at.most(m.price.max.moneyAmount.amount);\r",
									"    });\r",
									"});\r",
									"\r",
									"pm.test(\"Currency is USD for all modes\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode;\r",
									"\r",
									"    modes.forEach(m => {\r",
									"        pm.expect(m.price.min.moneyAmount.currency).to.eql(\"USD\");\r",
									"        pm.expect(m.price.max.moneyAmount.currency).to.eql(\"USD\");\r",
									"    });\r",
									"});\r",
									"\r",
									"pm.test(\"Transit time min is less than or equal to max\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode;\r",
									"\r",
									"    modes.forEach(m => {\r",
									"        pm.expect(m.transitTimes.min)\r",
									"            .to.be.at.most(m.transitTimes.max);\r",
									"    });\r",
									"});\r",
									"\r",
									"pm.test(\"Transit time unit is days\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode;\r",
									"\r",
									"    modes.forEach(m => {\r",
									"        pm.expect(m.transitTimes.unit).to.eql(\"days\");\r",
									"    });\r",
									"});\r",
									"\r",
									"pm.test(\"Number of quotes matches mode count\", function () {\r",
									"    const data = pm.response.json().response.estimatedFreightRates;\r",
									"    pm.expect(data.numQuotes).to.eql(data.mode.length);\r",
									"});\r",
									""
								],
								"type": "text/javascript",
								"packages": {}
							}
						}
					],
					"request": {
						"method": "GET",
						"header": [],
						"url": {
							"raw": "{{URL}}loadtype=boxes&weight=400&width=60&length=60&height=50&origin=Delhi,India&quantity=2&destination=Hamburg,Germany",
							"host": [
								"{{URL}}loadtype=boxes&weight=400&width=60&length=60&height=50&origin=Delhi,India&quantity=2&destination=Hamburg,Germany"
							]
						}
					},
					"response": []
				},
				{
					"name": "Box Shipment - Orgn=DE & Dst=IN",
					"event": [
						{
							"listen": "test",
							"script": {
								"exec": [
									"pm.test(\"Status code is 200\", function () {\r",
									"    pm.response.to.have.status(200);\r",
									"});\r",
									"\r",
									"/*pm.test(\"Response time is below 3000ms\", function () {\r",
									"    pm.expect(pm.response.responseTime).to.be.below(3000);\r",
									"});*/\r",
									"\r",
									"pm.test(\"Response object exists\", function () {\r",
									"    const jsonData = pm.response.json();\r",
									"    pm.expect(jsonData).to.have.property(\"response\");\r",
									"});\r",
									"\r",
									"pm.test(\"estimatedFreightRates is present\", function () {\r",
									"    const jsonData = pm.response.json();\r",
									"    pm.expect(jsonData.response).to.have.property(\"estimatedFreightRates\");\r",
									"});"
								],
								"type": "text/javascript"
							}
						}
					],
					"request": {
						"method": "GET",
						"header": [],
						"url": {
							"raw": "{{URL}}loadtype=boxes&weight=400&width=60&length=60&height=50&origin=Hamburg,Germany&quantity=2&destination=Delhi,India",
							"host": [
								"{{URL}}loadtype=boxes&weight=400&width=60&length=60&height=50&origin=Hamburg,Germany&quantity=2&destination=Delhi,India"
							]
						}
					},
					"response": []
				}
			]
		},
		{
			"name": "Freightos - shippingCalculator [Container Shipment] - XML",
			"item": [
				{
					"name": "Container Shipment - Orgn=CNSHA & Dst=USLGB",
					"event": [
						{
							"listen": "test",
							"script": {
								"exec": [
									"// Get XML response as text\r",
									"let xml = pm.response.text();\r",
									"\r",
									"// 1. Status code is 200\r",
									"pm.test(\"Status code is 200\", () => pm.response.to.have.status(200));\r",
									"\r",
									"// 2. estimatedFreightRates exists\r",
									"pm.test(\"estimatedFreightRates exists\", () => {\r",
									"    pm.expect(/<estimatedFreightRates/.test(xml)).to.be.true;\r",
									"});\r",
									"\r",
									"// 3. Number of quotes\r",
									"pm.test(\"Number of quotes is >= 0\", () => {\r",
									"    let match = xml.match(/<estimatedFreightRates[^>]*numQuotes=\"(\\d+)\"/);\r",
									"    pm.expect(match).to.not.be.null;\r",
									"    let numQuotes = parseInt(match[1]);\r",
									"    pm.expect(numQuotes).to.be.at.least(0);\r",
									"});\r",
									"\r",
									"// Only run detailed tests if there are quotes\r",
									"let numQuotes = parseInt(xml.match(/<estimatedFreightRates[^>]*numQuotes=\"(\\d+)\"/)[1]);\r",
									"if (numQuotes > 0) {\r",
									"\r",
									"    // Match all <mode> blocks\r",
									"    let modeMatches = [...xml.matchAll(/<mode[^>]*mode=\"([^\"]+)\"[\\s\\S]*?<\\/mode>/g)];\r",
									"    pm.test(\"At least one mode exists\", () => pm.expect(modeMatches.length).to.be.at.least(1));\r",
									"\r",
									"    modeMatches.forEach((modeMatch, index) => {\r",
									"        let modeBlock = modeMatch[0];\r",
									"        let modeName = modeMatch[1];\r",
									"\r",
									"        // Mode validation\r",
									"        pm.test(`Mode #${index + 1} is FCL`, () => pm.expect(modeName).to.eql(\"FCL\"));\r",
									"\r",
									"        // Prices\r",
									"        let minPrice = parseFloat(modeBlock.match(/<min>\\s*<moneyAmount[^>]*amount=\"([^\"]+)\"/)[1]);\r",
									"        let maxPrice = parseFloat(modeBlock.match(/<max>\\s*<moneyAmount[^>]*amount=\"([^\"]+)\"/)[1]);\r",
									"        pm.test(`Mode #${index + 1} prices exist`, () => {\r",
									"            pm.expect(minPrice).to.not.be.NaN;\r",
									"            pm.expect(maxPrice).to.not.be.NaN;\r",
									"        });\r",
									"        pm.test(`Mode #${index + 1} price validations`, () => {\r",
									"            pm.expect(minPrice).to.be.at.least(0);\r",
									"            pm.expect(maxPrice).to.be.at.least(minPrice);\r",
									"        });\r",
									"\r",
									"        // Currency\r",
									"        let minCurrency = modeBlock.match(/<min>\\s*<moneyAmount[^>]*currency=\"([^\"]+)\"/)[1];\r",
									"        let maxCurrency = modeBlock.match(/<max>\\s*<moneyAmount[^>]*currency=\"([^\"]+)\"/)[1];\r",
									"        pm.test(`Mode #${index + 1} currency is USD`, () => {\r",
									"            pm.expect(minCurrency).to.eql(\"USD\");\r",
									"            pm.expect(maxCurrency).to.eql(\"USD\");\r",
									"        });\r",
									"\r",
									"        // Transit times\r",
									"        let minDays = parseInt(modeBlock.match(/<transitTimes[^>]*>[\\s\\S]*<min>(\\d+)<\\/min>/)[1]);\r",
									"        let maxDays = parseInt(modeBlock.match(/<transitTimes[^>]*>[\\s\\S]*<max>(\\d+)<\\/max>/)[1]);\r",
									"        pm.test(`Mode #${index + 1} transit times exist`, () => {\r",
									"            pm.expect(minDays).to.not.be.NaN;\r",
									"            pm.expect(maxDays).to.not.be.NaN;\r",
									"        });\r",
									"        pm.test(`Mode #${index + 1} transit min <= max`, () => {\r",
									"            pm.expect(maxDays).to.be.at.least(minDays);\r",
									"        });\r",
									"    });\r",
									"} else {\r",
									"    pm.test(\"No quotes available, skipping detailed mode tests\", () => true);\r",
									"}"
								],
								"type": "text/javascript",
								"packages": {}
							}
						}
					],
					"request": {
						"method": "GET",
						"header": [],
						"url": {
							"raw": "{{URL}}format=xml&loadtype=container40&weight=15000&origin=CNSHA&quantity=2&destination=USLGB",
							"host": [
								"{{URL}}format=xml&loadtype=container40&weight=15000&origin=CNSHA&quantity=2&destination=USLGB"
							]
						}
					},
					"response": []
				},
				{
					"name": "Container Shipment - Orgn=CNSHA & Dst=USLGB (1)",
					"event": [
						{
							"listen": "test",
							"script": {
								"exec": [
									"// Get XML response as text\r",
									"let xml = pm.response.text();\r",
									"\r",
									"// 1. Status code is 200\r",
									"pm.test(\"Status code is 200\", () => pm.response.to.have.status(200));\r",
									"\r",
									"// 2. estimatedFreightRates exists\r",
									"pm.test(\"estimatedFreightRates exists\", () => {\r",
									"    pm.expect(/<estimatedFreightRates/.test(xml)).to.be.true;\r",
									"});\r",
									"\r",
									"// 3. Number of quotes\r",
									"pm.test(\"Number of quotes is >= 0\", () => {\r",
									"    let match = xml.match(/<estimatedFreightRates[^>]*numQuotes=\"(\\d+)\"/);\r",
									"    pm.expect(match).to.not.be.null;\r",
									"    let numQuotes = parseInt(match[1]);\r",
									"    pm.expect(numQuotes).to.be.at.least(0);\r",
									"});\r",
									"\r",
									"// Only run detailed tests if there are quotes\r",
									"let numQuotes = parseInt(xml.match(/<estimatedFreightRates[^>]*numQuotes=\"(\\d+)\"/)[1]);\r",
									"if (numQuotes > 0) {\r",
									"\r",
									"    // Match all <mode> blocks\r",
									"    let modeMatches = [...xml.matchAll(/<mode[^>]*mode=\"([^\"]+)\"[\\s\\S]*?<\\/mode>/g)];\r",
									"    pm.test(\"At least one mode exists\", () => pm.expect(modeMatches.length).to.be.at.least(1));\r",
									"\r",
									"    modeMatches.forEach((modeMatch, index) => {\r",
									"        let modeBlock = modeMatch[0];\r",
									"        let modeName = modeMatch[1];\r",
									"\r",
									"        // Mode validation\r",
									"        pm.test(`Mode #${index + 1} is FCL`, () => pm.expect(modeName).to.eql(\"FCL\"));\r",
									"\r",
									"        // Prices\r",
									"        let minPrice = parseFloat(modeBlock.match(/<min>\\s*<moneyAmount[^>]*amount=\"([^\"]+)\"/)[1]);\r",
									"        let maxPrice = parseFloat(modeBlock.match(/<max>\\s*<moneyAmount[^>]*amount=\"([^\"]+)\"/)[1]);\r",
									"        pm.test(`Mode #${index + 1} prices exist`, () => {\r",
									"            pm.expect(minPrice).to.not.be.NaN;\r",
									"            pm.expect(maxPrice).to.not.be.NaN;\r",
									"        });\r",
									"        pm.test(`Mode #${index + 1} price validations`, () => {\r",
									"            pm.expect(minPrice).to.be.at.least(0);\r",
									"            pm.expect(maxPrice).to.be.at.least(minPrice);\r",
									"        });\r",
									"\r",
									"        // Currency\r",
									"        let minCurrency = modeBlock.match(/<min>\\s*<moneyAmount[^>]*currency=\"([^\"]+)\"/)[1];\r",
									"        let maxCurrency = modeBlock.match(/<max>\\s*<moneyAmount[^>]*currency=\"([^\"]+)\"/)[1];\r",
									"        pm.test(`Mode #${index + 1} currency is USD`, () => {\r",
									"            pm.expect(minCurrency).to.eql(\"USD\");\r",
									"            pm.expect(maxCurrency).to.eql(\"USD\");\r",
									"        });\r",
									"\r",
									"        // Transit times\r",
									"        let minDays = parseInt(modeBlock.match(/<transitTimes[^>]*>[\\s\\S]*<min>(\\d+)<\\/min>/)[1]);\r",
									"        let maxDays = parseInt(modeBlock.match(/<transitTimes[^>]*>[\\s\\S]*<max>(\\d+)<\\/max>/)[1]);\r",
									"        pm.test(`Mode #${index + 1} transit times exist`, () => {\r",
									"            pm.expect(minDays).to.not.be.NaN;\r",
									"            pm.expect(maxDays).to.not.be.NaN;\r",
									"        });\r",
									"        pm.test(`Mode #${index + 1} transit min <= max`, () => {\r",
									"            pm.expect(maxDays).to.be.at.least(minDays);\r",
									"        });\r",
									"    });\r",
									"} else {\r",
									"    pm.test(\"No quotes available, skipping detailed mode tests\", () => true);\r",
									"}\r",
									""
								],
								"type": "text/javascript",
								"packages": {}
							}
						}
					],
					"request": {
						"method": "GET",
						"header": [],
						"url": {
							"raw": "{{URL}}format=xml&loadtype=container40&weight=12000&origin=CNSHA&quantity=1&destination=USLGB",
							"host": [
								"{{URL}}format=xml&loadtype=container40&weight=12000&origin=CNSHA&quantity=1&destination=USLGB"
							]
						}
					},
					"response": []
				},
				{
					"name": "Container Shipment - Orgn=CNSHA & Dst=USLGB (2)",
					"event": [
						{
							"listen": "test",
							"script": {
								"exec": [
									"// Get XML response as text\r",
									"let xml = pm.response.text();\r",
									"\r",
									"// 1. Status code is 200\r",
									"pm.test(\"Status code is 200\", () => pm.response.to.have.status(200));\r",
									"\r",
									"// 2. estimatedFreightRates exists\r",
									"pm.test(\"estimatedFreightRates exists\", () => {\r",
									"    pm.expect(/<estimatedFreightRates/.test(xml)).to.be.true;\r",
									"});\r",
									"\r",
									"// 3. Number of quotes\r",
									"pm.test(\"Number of quotes is >= 0\", () => {\r",
									"    let match = xml.match(/<estimatedFreightRates[^>]*numQuotes=\"(\\d+)\"/);\r",
									"    pm.expect(match).to.not.be.null;\r",
									"    let numQuotes = parseInt(match[1]);\r",
									"    pm.expect(numQuotes).to.be.at.least(0);\r",
									"});\r",
									"\r",
									"// Only run detailed tests if there are quotes\r",
									"let numQuotes = parseInt(xml.match(/<estimatedFreightRates[^>]*numQuotes=\"(\\d+)\"/)[1]);\r",
									"if (numQuotes > 0) {\r",
									"\r",
									"    // Match all <mode> blocks\r",
									"    let modeMatches = [...xml.matchAll(/<mode[^>]*mode=\"([^\"]+)\"[\\s\\S]*?<\\/mode>/g)];\r",
									"    pm.test(\"At least one mode exists\", () => pm.expect(modeMatches.length).to.be.at.least(1));\r",
									"\r",
									"    modeMatches.forEach((modeMatch, index) => {\r",
									"        let modeBlock = modeMatch[0];\r",
									"        let modeName = modeMatch[1];\r",
									"\r",
									"        // Mode validation\r",
									"        pm.test(`Mode #${index + 1} is FCL`, () => pm.expect(modeName).to.eql(\"FCL\"));\r",
									"\r",
									"        // Prices\r",
									"        let minPrice = parseFloat(modeBlock.match(/<min>\\s*<moneyAmount[^>]*amount=\"([^\"]+)\"/)[1]);\r",
									"        let maxPrice = parseFloat(modeBlock.match(/<max>\\s*<moneyAmount[^>]*amount=\"([^\"]+)\"/)[1]);\r",
									"        pm.test(`Mode #${index + 1} prices exist`, () => {\r",
									"            pm.expect(minPrice).to.not.be.NaN;\r",
									"            pm.expect(maxPrice).to.not.be.NaN;\r",
									"        });\r",
									"        pm.test(`Mode #${index + 1} price validations`, () => {\r",
									"            pm.expect(minPrice).to.be.at.least(0);\r",
									"            pm.expect(maxPrice).to.be.at.least(minPrice);\r",
									"        });\r",
									"\r",
									"        // Currency\r",
									"        let minCurrency = modeBlock.match(/<min>\\s*<moneyAmount[^>]*currency=\"([^\"]+)\"/)[1];\r",
									"        let maxCurrency = modeBlock.match(/<max>\\s*<moneyAmount[^>]*currency=\"([^\"]+)\"/)[1];\r",
									"        pm.test(`Mode #${index + 1} currency is USD`, () => {\r",
									"            pm.expect(minCurrency).to.eql(\"USD\");\r",
									"            pm.expect(maxCurrency).to.eql(\"USD\");\r",
									"        });\r",
									"\r",
									"        // Transit times\r",
									"        let minDays = parseInt(modeBlock.match(/<transitTimes[^>]*>[\\s\\S]*<min>(\\d+)<\\/min>/)[1]);\r",
									"        let maxDays = parseInt(modeBlock.match(/<transitTimes[^>]*>[\\s\\S]*<max>(\\d+)<\\/max>/)[1]);\r",
									"        pm.test(`Mode #${index + 1} transit times exist`, () => {\r",
									"            pm.expect(minDays).to.not.be.NaN;\r",
									"            pm.expect(maxDays).to.not.be.NaN;\r",
									"        });\r",
									"        pm.test(`Mode #${index + 1} transit min <= max`, () => {\r",
									"            pm.expect(maxDays).to.be.at.least(minDays);\r",
									"        });\r",
									"    });\r",
									"} else {\r",
									"    pm.test(\"No quotes available, skipping detailed mode tests\", () => true);\r",
									"}"
								],
								"type": "text/javascript",
								"packages": {}
							}
						}
					],
					"request": {
						"method": "GET",
						"header": [],
						"url": {
							"raw": "{{URL}}format=xml&loadtype=container40&weight=15000&origin=CNSHA&quantity=3&destination=USLGB",
							"host": [
								"{{URL}}format=xml&loadtype=container40&weight=15000&origin=CNSHA&quantity=3&destination=USLGB"
							]
						}
					},
					"response": []
				},
				{
					"name": "Container Shipment - Orgn=CNSHA & Dst=USLGB (3)",
					"event": [
						{
							"listen": "test",
							"script": {
								"exec": [
									"// Get XML response as text\r",
									"let xml = pm.response.text();\r",
									"\r",
									"// 1. Status code is 200\r",
									"pm.test(\"Status code is 200\", () => pm.response.to.have.status(200));\r",
									"\r",
									"// 2. estimatedFreightRates exists\r",
									"pm.test(\"estimatedFreightRates exists\", () => {\r",
									"    pm.expect(/<estimatedFreightRates/.test(xml)).to.be.true;\r",
									"});\r",
									"\r",
									"// 3. Number of quotes\r",
									"pm.test(\"Number of quotes is >= 0\", () => {\r",
									"    let match = xml.match(/<estimatedFreightRates[^>]*numQuotes=\"(\\d+)\"/);\r",
									"    pm.expect(match).to.not.be.null;\r",
									"    let numQuotes = parseInt(match[1]);\r",
									"    pm.expect(numQuotes).to.be.at.least(0);\r",
									"});\r",
									"\r",
									"// Only run detailed tests if there are quotes\r",
									"let numQuotes = parseInt(xml.match(/<estimatedFreightRates[^>]*numQuotes=\"(\\d+)\"/)[1]);\r",
									"if (numQuotes > 0) {\r",
									"\r",
									"    // Match all <mode> blocks\r",
									"    let modeMatches = [...xml.matchAll(/<mode[^>]*mode=\"([^\"]+)\"[\\s\\S]*?<\\/mode>/g)];\r",
									"    pm.test(\"At least one mode exists\", () => pm.expect(modeMatches.length).to.be.at.least(1));\r",
									"\r",
									"    modeMatches.forEach((modeMatch, index) => {\r",
									"        let modeBlock = modeMatch[0];\r",
									"        let modeName = modeMatch[1];\r",
									"\r",
									"        // Mode validation\r",
									"        pm.test(`Mode #${index + 1} is FCL`, () => pm.expect(modeName).to.eql(\"FCL\"));\r",
									"\r",
									"        // Prices\r",
									"        let minPrice = parseFloat(modeBlock.match(/<min>\\s*<moneyAmount[^>]*amount=\"([^\"]+)\"/)[1]);\r",
									"        let maxPrice = parseFloat(modeBlock.match(/<max>\\s*<moneyAmount[^>]*amount=\"([^\"]+)\"/)[1]);\r",
									"        pm.test(`Mode #${index + 1} prices exist`, () => {\r",
									"            pm.expect(minPrice).to.not.be.NaN;\r",
									"            pm.expect(maxPrice).to.not.be.NaN;\r",
									"        });\r",
									"        pm.test(`Mode #${index + 1} price validations`, () => {\r",
									"            pm.expect(minPrice).to.be.at.least(0);\r",
									"            pm.expect(maxPrice).to.be.at.least(minPrice);\r",
									"        });\r",
									"\r",
									"        // Currency\r",
									"        let minCurrency = modeBlock.match(/<min>\\s*<moneyAmount[^>]*currency=\"([^\"]+)\"/)[1];\r",
									"        let maxCurrency = modeBlock.match(/<max>\\s*<moneyAmount[^>]*currency=\"([^\"]+)\"/)[1];\r",
									"        pm.test(`Mode #${index + 1} currency is USD`, () => {\r",
									"            pm.expect(minCurrency).to.eql(\"USD\");\r",
									"            pm.expect(maxCurrency).to.eql(\"USD\");\r",
									"        });\r",
									"\r",
									"        // Transit times\r",
									"        let minDays = parseInt(modeBlock.match(/<transitTimes[^>]*>[\\s\\S]*<min>(\\d+)<\\/min>/)[1]);\r",
									"        let maxDays = parseInt(modeBlock.match(/<transitTimes[^>]*>[\\s\\S]*<max>(\\d+)<\\/max>/)[1]);\r",
									"        pm.test(`Mode #${index + 1} transit times exist`, () => {\r",
									"            pm.expect(minDays).to.not.be.NaN;\r",
									"            pm.expect(maxDays).to.not.be.NaN;\r",
									"        });\r",
									"        pm.test(`Mode #${index + 1} transit min <= max`, () => {\r",
									"            pm.expect(maxDays).to.be.at.least(minDays);\r",
									"        });\r",
									"    });\r",
									"} else {\r",
									"    pm.test(\"No quotes available, skipping detailed mode tests\", () => true);\r",
									"}"
								],
								"type": "text/javascript"
							}
						}
					],
					"request": {
						"method": "GET",
						"header": [],
						"url": {
							"raw": "{{URL}}format=xml&loadtype=container40&weight=18300&origin=CNSHA&quantity=3&destination=USLGB",
							"host": [
								"{{URL}}format=xml&loadtype=container40&weight=18300&origin=CNSHA&quantity=3&destination=USLGB"
							]
						}
					},
					"response": []
				},
				{
					"name": "Container Shipment - Orgn=CNSHA & Dst=USLGB (4)",
					"event": [
						{
							"listen": "test",
							"script": {
								"exec": [
									"// Get XML response as text\r",
									"let xml = pm.response.text();\r",
									"\r",
									"// 1. Status code is 200\r",
									"pm.test(\"Status code is 200\", () => pm.response.to.have.status(200));\r",
									"\r",
									"// 2. estimatedFreightRates exists\r",
									"pm.test(\"estimatedFreightRates exists\", () => {\r",
									"    pm.expect(/<estimatedFreightRates/.test(xml)).to.be.true;\r",
									"});\r",
									"\r",
									"// 3. Number of quotes\r",
									"pm.test(\"Number of quotes is >= 0\", () => {\r",
									"    let match = xml.match(/<estimatedFreightRates[^>]*numQuotes=\"(\\d+)\"/);\r",
									"    pm.expect(match).to.not.be.null;\r",
									"    let numQuotes = parseInt(match[1]);\r",
									"    pm.expect(numQuotes).to.be.at.least(0);\r",
									"});\r",
									"\r",
									"// Only run detailed tests if there are quotes\r",
									"let numQuotes = parseInt(xml.match(/<estimatedFreightRates[^>]*numQuotes=\"(\\d+)\"/)[1]);\r",
									"if (numQuotes > 0) {\r",
									"\r",
									"    // Match all <mode> blocks\r",
									"    let modeMatches = [...xml.matchAll(/<mode[^>]*mode=\"([^\"]+)\"[\\s\\S]*?<\\/mode>/g)];\r",
									"    pm.test(\"At least one mode exists\", () => pm.expect(modeMatches.length).to.be.at.least(1));\r",
									"\r",
									"    modeMatches.forEach((modeMatch, index) => {\r",
									"        let modeBlock = modeMatch[0];\r",
									"        let modeName = modeMatch[1];\r",
									"\r",
									"        // Mode validation\r",
									"        pm.test(`Mode #${index + 1} is FCL`, () => pm.expect(modeName).to.eql(\"FCL\"));\r",
									"\r",
									"        // Prices\r",
									"        let minPrice = parseFloat(modeBlock.match(/<min>\\s*<moneyAmount[^>]*amount=\"([^\"]+)\"/)[1]);\r",
									"        let maxPrice = parseFloat(modeBlock.match(/<max>\\s*<moneyAmount[^>]*amount=\"([^\"]+)\"/)[1]);\r",
									"        pm.test(`Mode #${index + 1} prices exist`, () => {\r",
									"            pm.expect(minPrice).to.not.be.NaN;\r",
									"            pm.expect(maxPrice).to.not.be.NaN;\r",
									"        });\r",
									"        pm.test(`Mode #${index + 1} price validations`, () => {\r",
									"            pm.expect(minPrice).to.be.at.least(0);\r",
									"            pm.expect(maxPrice).to.be.at.least(minPrice);\r",
									"        });\r",
									"\r",
									"        // Currency\r",
									"        let minCurrency = modeBlock.match(/<min>\\s*<moneyAmount[^>]*currency=\"([^\"]+)\"/)[1];\r",
									"        let maxCurrency = modeBlock.match(/<max>\\s*<moneyAmount[^>]*currency=\"([^\"]+)\"/)[1];\r",
									"        pm.test(`Mode #${index + 1} currency is USD`, () => {\r",
									"            pm.expect(minCurrency).to.eql(\"USD\");\r",
									"            pm.expect(maxCurrency).to.eql(\"USD\");\r",
									"        });\r",
									"\r",
									"        // Transit times\r",
									"        let minDays = parseInt(modeBlock.match(/<transitTimes[^>]*>[\\s\\S]*<min>(\\d+)<\\/min>/)[1]);\r",
									"        let maxDays = parseInt(modeBlock.match(/<transitTimes[^>]*>[\\s\\S]*<max>(\\d+)<\\/max>/)[1]);\r",
									"        pm.test(`Mode #${index + 1} transit times exist`, () => {\r",
									"            pm.expect(minDays).to.not.be.NaN;\r",
									"            pm.expect(maxDays).to.not.be.NaN;\r",
									"        });\r",
									"        pm.test(`Mode #${index + 1} transit min <= max`, () => {\r",
									"            pm.expect(maxDays).to.be.at.least(minDays);\r",
									"        });\r",
									"    });\r",
									"} else {\r",
									"    pm.test(\"No quotes available, skipping detailed mode tests\", () => true);\r",
									"}"
								],
								"type": "text/javascript"
							}
						}
					],
					"request": {
						"method": "GET",
						"header": [],
						"url": {
							"raw": "{{URL}}format=xml&loadtype=container40&weight=17700&origin=CNSHA&quantity=3&destination=USLGB",
							"host": [
								"{{URL}}format=xml&loadtype=container40&weight=17700&origin=CNSHA&quantity=3&destination=USLGB"
							]
						}
					},
					"response": []
				},
				{
					"name": "Container Shipment - Orgn=HKHKG & Dst=USNYC",
					"event": [
						{
							"listen": "test",
							"script": {
								"exec": [
									"// Get XML response as text\r",
									"let xml = pm.response.text();\r",
									"\r",
									"// 1. Status code is 200\r",
									"pm.test(\"Status code is 200\", () => pm.response.to.have.status(200));\r",
									"\r",
									"// 2. estimatedFreightRates exists\r",
									"pm.test(\"estimatedFreightRates exists\", () => {\r",
									"    pm.expect(/<estimatedFreightRates/.test(xml)).to.be.true;\r",
									"});\r",
									"\r",
									"// 3. Number of quotes\r",
									"pm.test(\"Number of quotes is >= 0\", () => {\r",
									"    let match = xml.match(/<estimatedFreightRates[^>]*numQuotes=\"(\\d+)\"/);\r",
									"    pm.expect(match).to.not.be.null;\r",
									"    let numQuotes = parseInt(match[1]);\r",
									"    pm.expect(numQuotes).to.be.at.least(0);\r",
									"});\r",
									"\r",
									"// Only run detailed tests if there are quotes\r",
									"let numQuotes = parseInt(xml.match(/<estimatedFreightRates[^>]*numQuotes=\"(\\d+)\"/)[1]);\r",
									"if (numQuotes > 0) {\r",
									"\r",
									"    // Match all <mode> blocks\r",
									"    let modeMatches = [...xml.matchAll(/<mode[^>]*mode=\"([^\"]+)\"[\\s\\S]*?<\\/mode>/g)];\r",
									"    pm.test(\"At least one mode exists\", () => pm.expect(modeMatches.length).to.be.at.least(1));\r",
									"\r",
									"    modeMatches.forEach((modeMatch, index) => {\r",
									"        let modeBlock = modeMatch[0];\r",
									"        let modeName = modeMatch[1];\r",
									"\r",
									"        // Mode validation\r",
									"        pm.test(`Mode #${index + 1} is FCL`, () => pm.expect(modeName).to.eql(\"FCL\"));\r",
									"\r",
									"        // Prices\r",
									"        let minPrice = parseFloat(modeBlock.match(/<min>\\s*<moneyAmount[^>]*amount=\"([^\"]+)\"/)[1]);\r",
									"        let maxPrice = parseFloat(modeBlock.match(/<max>\\s*<moneyAmount[^>]*amount=\"([^\"]+)\"/)[1]);\r",
									"        pm.test(`Mode #${index + 1} prices exist`, () => {\r",
									"            pm.expect(minPrice).to.not.be.NaN;\r",
									"            pm.expect(maxPrice).to.not.be.NaN;\r",
									"        });\r",
									"        pm.test(`Mode #${index + 1} price validations`, () => {\r",
									"            pm.expect(minPrice).to.be.at.least(0);\r",
									"            pm.expect(maxPrice).to.be.at.least(minPrice);\r",
									"        });\r",
									"\r",
									"        // Currency\r",
									"        let minCurrency = modeBlock.match(/<min>\\s*<moneyAmount[^>]*currency=\"([^\"]+)\"/)[1];\r",
									"        let maxCurrency = modeBlock.match(/<max>\\s*<moneyAmount[^>]*currency=\"([^\"]+)\"/)[1];\r",
									"        pm.test(`Mode #${index + 1} currency is USD`, () => {\r",
									"            pm.expect(minCurrency).to.eql(\"USD\");\r",
									"            pm.expect(maxCurrency).to.eql(\"USD\");\r",
									"        });\r",
									"\r",
									"        // Transit times\r",
									"        let minDays = parseInt(modeBlock.match(/<transitTimes[^>]*>[\\s\\S]*<min>(\\d+)<\\/min>/)[1]);\r",
									"        let maxDays = parseInt(modeBlock.match(/<transitTimes[^>]*>[\\s\\S]*<max>(\\d+)<\\/max>/)[1]);\r",
									"        pm.test(`Mode #${index + 1} transit times exist`, () => {\r",
									"            pm.expect(minDays).to.not.be.NaN;\r",
									"            pm.expect(maxDays).to.not.be.NaN;\r",
									"        });\r",
									"        pm.test(`Mode #${index + 1} transit min <= max`, () => {\r",
									"            pm.expect(maxDays).to.be.at.least(minDays);\r",
									"        });\r",
									"    });\r",
									"} else {\r",
									"    pm.test(\"No quotes available, skipping detailed mode tests\", () => true);\r",
									"}"
								],
								"type": "text/javascript",
								"packages": {}
							}
						}
					],
					"request": {
						"method": "GET",
						"header": [],
						"url": {
							"raw": "{{URL}}format=xml&loadtype=container40&weight=10000&origin=HKHKG&quantity=1&destination=USNYC",
							"host": [
								"{{URL}}format=xml&loadtype=container40&weight=10000&origin=HKHKG&quantity=1&destination=USNYC"
							]
						}
					},
					"response": []
				},
				{
					"name": "Container Shipment - Orgn=HKHKG & Dst=USNYC (1)",
					"event": [
						{
							"listen": "test",
							"script": {
								"exec": [
									"// Get XML response as text\r",
									"let xml = pm.response.text();\r",
									"\r",
									"// 1. Status code is 200\r",
									"pm.test(\"Status code is 200\", () => pm.response.to.have.status(200));\r",
									"\r",
									"// 2. estimatedFreightRates exists\r",
									"pm.test(\"estimatedFreightRates exists\", () => {\r",
									"    pm.expect(/<estimatedFreightRates/.test(xml)).to.be.true;\r",
									"});\r",
									"\r",
									"// 3. Number of quotes\r",
									"pm.test(\"Number of quotes is >= 0\", () => {\r",
									"    let match = xml.match(/<estimatedFreightRates[^>]*numQuotes=\"(\\d+)\"/);\r",
									"    pm.expect(match).to.not.be.null;\r",
									"    let numQuotes = parseInt(match[1]);\r",
									"    pm.expect(numQuotes).to.be.at.least(0);\r",
									"});\r",
									"\r",
									"// Only run detailed tests if there are quotes\r",
									"let numQuotes = parseInt(xml.match(/<estimatedFreightRates[^>]*numQuotes=\"(\\d+)\"/)[1]);\r",
									"if (numQuotes > 0) {\r",
									"\r",
									"    // Match all <mode> blocks\r",
									"    let modeMatches = [...xml.matchAll(/<mode[^>]*mode=\"([^\"]+)\"[\\s\\S]*?<\\/mode>/g)];\r",
									"    pm.test(\"At least one mode exists\", () => pm.expect(modeMatches.length).to.be.at.least(1));\r",
									"\r",
									"    modeMatches.forEach((modeMatch, index) => {\r",
									"        let modeBlock = modeMatch[0];\r",
									"        let modeName = modeMatch[1];\r",
									"\r",
									"        // Mode validation\r",
									"        pm.test(`Mode #${index + 1} is FCL`, () => pm.expect(modeName).to.eql(\"FCL\"));\r",
									"\r",
									"        // Prices\r",
									"        let minPrice = parseFloat(modeBlock.match(/<min>\\s*<moneyAmount[^>]*amount=\"([^\"]+)\"/)[1]);\r",
									"        let maxPrice = parseFloat(modeBlock.match(/<max>\\s*<moneyAmount[^>]*amount=\"([^\"]+)\"/)[1]);\r",
									"        pm.test(`Mode #${index + 1} prices exist`, () => {\r",
									"            pm.expect(minPrice).to.not.be.NaN;\r",
									"            pm.expect(maxPrice).to.not.be.NaN;\r",
									"        });\r",
									"        pm.test(`Mode #${index + 1} price validations`, () => {\r",
									"            pm.expect(minPrice).to.be.at.least(0);\r",
									"            pm.expect(maxPrice).to.be.at.least(minPrice);\r",
									"        });\r",
									"\r",
									"        // Currency\r",
									"        let minCurrency = modeBlock.match(/<min>\\s*<moneyAmount[^>]*currency=\"([^\"]+)\"/)[1];\r",
									"        let maxCurrency = modeBlock.match(/<max>\\s*<moneyAmount[^>]*currency=\"([^\"]+)\"/)[1];\r",
									"        pm.test(`Mode #${index + 1} currency is USD`, () => {\r",
									"            pm.expect(minCurrency).to.eql(\"USD\");\r",
									"            pm.expect(maxCurrency).to.eql(\"USD\");\r",
									"        });\r",
									"\r",
									"        // Transit times\r",
									"        let minDays = parseInt(modeBlock.match(/<transitTimes[^>]*>[\\s\\S]*<min>(\\d+)<\\/min>/)[1]);\r",
									"        let maxDays = parseInt(modeBlock.match(/<transitTimes[^>]*>[\\s\\S]*<max>(\\d+)<\\/max>/)[1]);\r",
									"        pm.test(`Mode #${index + 1} transit times exist`, () => {\r",
									"            pm.expect(minDays).to.not.be.NaN;\r",
									"            pm.expect(maxDays).to.not.be.NaN;\r",
									"        });\r",
									"        pm.test(`Mode #${index + 1} transit min <= max`, () => {\r",
									"            pm.expect(maxDays).to.be.at.least(minDays);\r",
									"        });\r",
									"    });\r",
									"} else {\r",
									"    pm.test(\"No quotes available, skipping detailed mode tests\", () => true);\r",
									"}"
								],
								"type": "text/javascript",
								"packages": {}
							}
						}
					],
					"request": {
						"method": "GET",
						"header": [],
						"url": {
							"raw": "{{URL}}format=xml&loadtype=container40&weight=12000&origin=HKHKG&quantity=1&destination=USNYC",
							"host": [
								"{{URL}}format=xml&loadtype=container40&weight=12000&origin=HKHKG&quantity=1&destination=USNYC"
							]
						}
					},
					"response": []
				},
				{
					"name": "Container Shipment - Orgn=HKHKG & Dst=USNYC (2)",
					"event": [
						{
							"listen": "test",
							"script": {
								"exec": [
									"// Get XML response as text\r",
									"let xml = pm.response.text();\r",
									"\r",
									"// 1. Status code is 200\r",
									"pm.test(\"Status code is 200\", () => pm.response.to.have.status(200));\r",
									"\r",
									"// 2. estimatedFreightRates exists\r",
									"pm.test(\"estimatedFreightRates exists\", () => {\r",
									"    pm.expect(/<estimatedFreightRates/.test(xml)).to.be.true;\r",
									"});\r",
									"\r",
									"// 3. Number of quotes\r",
									"pm.test(\"Number of quotes is >= 0\", () => {\r",
									"    let match = xml.match(/<estimatedFreightRates[^>]*numQuotes=\"(\\d+)\"/);\r",
									"    pm.expect(match).to.not.be.null;\r",
									"    let numQuotes = parseInt(match[1]);\r",
									"    pm.expect(numQuotes).to.be.at.least(0);\r",
									"});\r",
									"\r",
									"// Only run detailed tests if there are quotes\r",
									"let numQuotes = parseInt(xml.match(/<estimatedFreightRates[^>]*numQuotes=\"(\\d+)\"/)[1]);\r",
									"if (numQuotes > 0) {\r",
									"\r",
									"    // Match all <mode> blocks\r",
									"    let modeMatches = [...xml.matchAll(/<mode[^>]*mode=\"([^\"]+)\"[\\s\\S]*?<\\/mode>/g)];\r",
									"    pm.test(\"At least one mode exists\", () => pm.expect(modeMatches.length).to.be.at.least(1));\r",
									"\r",
									"    modeMatches.forEach((modeMatch, index) => {\r",
									"        let modeBlock = modeMatch[0];\r",
									"        let modeName = modeMatch[1];\r",
									"\r",
									"        // Mode validation\r",
									"        pm.test(`Mode #${index + 1} is FCL`, () => pm.expect(modeName).to.eql(\"FCL\"));\r",
									"\r",
									"        // Prices\r",
									"        let minPrice = parseFloat(modeBlock.match(/<min>\\s*<moneyAmount[^>]*amount=\"([^\"]+)\"/)[1]);\r",
									"        let maxPrice = parseFloat(modeBlock.match(/<max>\\s*<moneyAmount[^>]*amount=\"([^\"]+)\"/)[1]);\r",
									"        pm.test(`Mode #${index + 1} prices exist`, () => {\r",
									"            pm.expect(minPrice).to.not.be.NaN;\r",
									"            pm.expect(maxPrice).to.not.be.NaN;\r",
									"        });\r",
									"        pm.test(`Mode #${index + 1} price validations`, () => {\r",
									"            pm.expect(minPrice).to.be.at.least(0);\r",
									"            pm.expect(maxPrice).to.be.at.least(minPrice);\r",
									"        });\r",
									"\r",
									"        // Currency\r",
									"        let minCurrency = modeBlock.match(/<min>\\s*<moneyAmount[^>]*currency=\"([^\"]+)\"/)[1];\r",
									"        let maxCurrency = modeBlock.match(/<max>\\s*<moneyAmount[^>]*currency=\"([^\"]+)\"/)[1];\r",
									"        pm.test(`Mode #${index + 1} currency is USD`, () => {\r",
									"            pm.expect(minCurrency).to.eql(\"USD\");\r",
									"            pm.expect(maxCurrency).to.eql(\"USD\");\r",
									"        });\r",
									"\r",
									"        // Transit times\r",
									"        let minDays = parseInt(modeBlock.match(/<transitTimes[^>]*>[\\s\\S]*<min>(\\d+)<\\/min>/)[1]);\r",
									"        let maxDays = parseInt(modeBlock.match(/<transitTimes[^>]*>[\\s\\S]*<max>(\\d+)<\\/max>/)[1]);\r",
									"        pm.test(`Mode #${index + 1} transit times exist`, () => {\r",
									"            pm.expect(minDays).to.not.be.NaN;\r",
									"            pm.expect(maxDays).to.not.be.NaN;\r",
									"        });\r",
									"        pm.test(`Mode #${index + 1} transit min <= max`, () => {\r",
									"            pm.expect(maxDays).to.be.at.least(minDays);\r",
									"        });\r",
									"    });\r",
									"} else {\r",
									"    pm.test(\"No quotes available, skipping detailed mode tests\", () => true);\r",
									"}"
								],
								"type": "text/javascript"
							}
						}
					],
					"request": {
						"method": "GET",
						"header": [],
						"url": {
							"raw": "{{URL}}format=xml&loadtype=container40&weight=24000&origin=HKHKG&quantity=1&destination=USNYC",
							"host": [
								"{{URL}}format=xml&loadtype=container40&weight=24000&origin=HKHKG&quantity=1&destination=USNYC"
							]
						}
					},
					"response": []
				},
				{
					"name": "Container Shipment - Orgn=HKHKG & Dst=USNYC (3)",
					"event": [
						{
							"listen": "test",
							"script": {
								"exec": [
									"// Get XML response as text\r",
									"let xml = pm.response.text();\r",
									"\r",
									"// 1. Status code is 200\r",
									"pm.test(\"Status code is 200\", () => pm.response.to.have.status(200));\r",
									"\r",
									"// 2. estimatedFreightRates exists\r",
									"pm.test(\"estimatedFreightRates exists\", () => {\r",
									"    pm.expect(/<estimatedFreightRates/.test(xml)).to.be.true;\r",
									"});\r",
									"\r",
									"// 3. Number of quotes\r",
									"pm.test(\"Number of quotes is >= 0\", () => {\r",
									"    let match = xml.match(/<estimatedFreightRates[^>]*numQuotes=\"(\\d+)\"/);\r",
									"    pm.expect(match).to.not.be.null;\r",
									"    let numQuotes = parseInt(match[1]);\r",
									"    pm.expect(numQuotes).to.be.at.least(0);\r",
									"});\r",
									"\r",
									"// Only run detailed tests if there are quotes\r",
									"let numQuotes = parseInt(xml.match(/<estimatedFreightRates[^>]*numQuotes=\"(\\d+)\"/)[1]);\r",
									"if (numQuotes > 0) {\r",
									"\r",
									"    // Match all <mode> blocks\r",
									"    let modeMatches = [...xml.matchAll(/<mode[^>]*mode=\"([^\"]+)\"[\\s\\S]*?<\\/mode>/g)];\r",
									"    pm.test(\"At least one mode exists\", () => pm.expect(modeMatches.length).to.be.at.least(1));\r",
									"\r",
									"    modeMatches.forEach((modeMatch, index) => {\r",
									"        let modeBlock = modeMatch[0];\r",
									"        let modeName = modeMatch[1];\r",
									"\r",
									"        // Mode validation\r",
									"        pm.test(`Mode #${index + 1} is FCL`, () => pm.expect(modeName).to.eql(\"FCL\"));\r",
									"\r",
									"        // Prices\r",
									"        let minPrice = parseFloat(modeBlock.match(/<min>\\s*<moneyAmount[^>]*amount=\"([^\"]+)\"/)[1]);\r",
									"        let maxPrice = parseFloat(modeBlock.match(/<max>\\s*<moneyAmount[^>]*amount=\"([^\"]+)\"/)[1]);\r",
									"        pm.test(`Mode #${index + 1} prices exist`, () => {\r",
									"            pm.expect(minPrice).to.not.be.NaN;\r",
									"            pm.expect(maxPrice).to.not.be.NaN;\r",
									"        });\r",
									"        pm.test(`Mode #${index + 1} price validations`, () => {\r",
									"            pm.expect(minPrice).to.be.at.least(0);\r",
									"            pm.expect(maxPrice).to.be.at.least(minPrice);\r",
									"        });\r",
									"\r",
									"        // Currency\r",
									"        let minCurrency = modeBlock.match(/<min>\\s*<moneyAmount[^>]*currency=\"([^\"]+)\"/)[1];\r",
									"        let maxCurrency = modeBlock.match(/<max>\\s*<moneyAmount[^>]*currency=\"([^\"]+)\"/)[1];\r",
									"        pm.test(`Mode #${index + 1} currency is USD`, () => {\r",
									"            pm.expect(minCurrency).to.eql(\"USD\");\r",
									"            pm.expect(maxCurrency).to.eql(\"USD\");\r",
									"        });\r",
									"\r",
									"        // Transit times\r",
									"        let minDays = parseInt(modeBlock.match(/<transitTimes[^>]*>[\\s\\S]*<min>(\\d+)<\\/min>/)[1]);\r",
									"        let maxDays = parseInt(modeBlock.match(/<transitTimes[^>]*>[\\s\\S]*<max>(\\d+)<\\/max>/)[1]);\r",
									"        pm.test(`Mode #${index + 1} transit times exist`, () => {\r",
									"            pm.expect(minDays).to.not.be.NaN;\r",
									"            pm.expect(maxDays).to.not.be.NaN;\r",
									"        });\r",
									"        pm.test(`Mode #${index + 1} transit min <= max`, () => {\r",
									"            pm.expect(maxDays).to.be.at.least(minDays);\r",
									"        });\r",
									"    });\r",
									"} else {\r",
									"    pm.test(\"No quotes available, skipping detailed mode tests\", () => true);\r",
									"}"
								],
								"type": "text/javascript"
							}
						}
					],
					"request": {
						"method": "GET",
						"header": [],
						"url": {
							"raw": "{{URL}}format=xml&loadtype=container40&weight=12360&origin=HKHKG&quantity=1&destination=USNYC",
							"host": [
								"{{URL}}format=xml&loadtype=container40&weight=12360&origin=HKHKG&quantity=1&destination=USNYC"
							]
						}
					},
					"response": []
				},
				{
					"name": "Container Shipment - Orgn=HKHKG & Dst=USNYC (4)",
					"event": [
						{
							"listen": "test",
							"script": {
								"exec": [
									"// Get XML response as text\r",
									"let xml = pm.response.text();\r",
									"\r",
									"// 1. Status code is 200\r",
									"pm.test(\"Status code is 200\", () => pm.response.to.have.status(200));\r",
									"\r",
									"// 2. estimatedFreightRates exists\r",
									"pm.test(\"estimatedFreightRates exists\", () => {\r",
									"    pm.expect(/<estimatedFreightRates/.test(xml)).to.be.true;\r",
									"});\r",
									"\r",
									"// 3. Number of quotes\r",
									"pm.test(\"Number of quotes is >= 0\", () => {\r",
									"    let match = xml.match(/<estimatedFreightRates[^>]*numQuotes=\"(\\d+)\"/);\r",
									"    pm.expect(match).to.not.be.null;\r",
									"    let numQuotes = parseInt(match[1]);\r",
									"    pm.expect(numQuotes).to.be.at.least(0);\r",
									"});\r",
									"\r",
									"// Only run detailed tests if there are quotes\r",
									"let numQuotes = parseInt(xml.match(/<estimatedFreightRates[^>]*numQuotes=\"(\\d+)\"/)[1]);\r",
									"if (numQuotes > 0) {\r",
									"\r",
									"    // Match all <mode> blocks\r",
									"    let modeMatches = [...xml.matchAll(/<mode[^>]*mode=\"([^\"]+)\"[\\s\\S]*?<\\/mode>/g)];\r",
									"    pm.test(\"At least one mode exists\", () => pm.expect(modeMatches.length).to.be.at.least(1));\r",
									"\r",
									"    modeMatches.forEach((modeMatch, index) => {\r",
									"        let modeBlock = modeMatch[0];\r",
									"        let modeName = modeMatch[1];\r",
									"\r",
									"        // Mode validation\r",
									"        pm.test(`Mode #${index + 1} is FCL`, () => pm.expect(modeName).to.eql(\"FCL\"));\r",
									"\r",
									"        // Prices\r",
									"        let minPrice = parseFloat(modeBlock.match(/<min>\\s*<moneyAmount[^>]*amount=\"([^\"]+)\"/)[1]);\r",
									"        let maxPrice = parseFloat(modeBlock.match(/<max>\\s*<moneyAmount[^>]*amount=\"([^\"]+)\"/)[1]);\r",
									"        pm.test(`Mode #${index + 1} prices exist`, () => {\r",
									"            pm.expect(minPrice).to.not.be.NaN;\r",
									"            pm.expect(maxPrice).to.not.be.NaN;\r",
									"        });\r",
									"        pm.test(`Mode #${index + 1} price validations`, () => {\r",
									"            pm.expect(minPrice).to.be.at.least(0);\r",
									"            pm.expect(maxPrice).to.be.at.least(minPrice);\r",
									"        });\r",
									"\r",
									"        // Currency\r",
									"        let minCurrency = modeBlock.match(/<min>\\s*<moneyAmount[^>]*currency=\"([^\"]+)\"/)[1];\r",
									"        let maxCurrency = modeBlock.match(/<max>\\s*<moneyAmount[^>]*currency=\"([^\"]+)\"/)[1];\r",
									"        pm.test(`Mode #${index + 1} currency is USD`, () => {\r",
									"            pm.expect(minCurrency).to.eql(\"USD\");\r",
									"            pm.expect(maxCurrency).to.eql(\"USD\");\r",
									"        });\r",
									"\r",
									"        // Transit times\r",
									"        let minDays = parseInt(modeBlock.match(/<transitTimes[^>]*>[\\s\\S]*<min>(\\d+)<\\/min>/)[1]);\r",
									"        let maxDays = parseInt(modeBlock.match(/<transitTimes[^>]*>[\\s\\S]*<max>(\\d+)<\\/max>/)[1]);\r",
									"        pm.test(`Mode #${index + 1} transit times exist`, () => {\r",
									"            pm.expect(minDays).to.not.be.NaN;\r",
									"            pm.expect(maxDays).to.not.be.NaN;\r",
									"        });\r",
									"        pm.test(`Mode #${index + 1} transit min <= max`, () => {\r",
									"            pm.expect(maxDays).to.be.at.least(minDays);\r",
									"        });\r",
									"    });\r",
									"} else {\r",
									"    pm.test(\"No quotes available, skipping detailed mode tests\", () => true);\r",
									"}"
								],
								"type": "text/javascript"
							}
						}
					],
					"request": {
						"method": "GET",
						"header": [],
						"url": {
							"raw": "{{URL}}format=xml&loadtype=container40&weight=12834&origin=HKHKG&quantity=1&destination=USNYC",
							"host": [
								"{{URL}}format=xml&loadtype=container40&weight=12834&origin=HKHKG&quantity=1&destination=USNYC"
							]
						}
					},
					"response": []
				}
			]
		},
		{
			"name": "Freightos - shippingCalculator [Box & Container Shipment]",
			"item": [
				{
					"name": "Box Shipment - Orgn=CN & Dst=US",
					"event": [
						{
							"listen": "test",
							"script": {
								"exec": [
									"pm.test(\"Status code is 200\", function () {\r",
									"    pm.response.to.have.status(200);\r",
									"});\r",
									"\r",
									"/*pm.test(\"Response time is below 3000ms\", function () {\r",
									"    pm.expect(pm.response.responseTime).to.be.below(3000);\r",
									"});*/\r",
									"\r",
									"pm.test(\"Response object exists\", function () {\r",
									"    const jsonData = pm.response.json();\r",
									"    pm.expect(jsonData).to.have.property(\"response\");\r",
									"});\r",
									"\r",
									"pm.test(\"estimatedFreightRates is present\", function () {\r",
									"    const jsonData = pm.response.json();\r",
									"    pm.expect(jsonData.response).to.have.property(\"estimatedFreightRates\");\r",
									"});\r",
									"\r",
									"pm.test(\"Mode array is not empty\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode;\r",
									"    pm.expect(modes.length).to.be.greaterThan(0);\r",
									"});\r",
									"\r",
									"pm.test(\"Expected shipping modes are available\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode\r",
									"        .map(m => m.mode);\r",
									"\r",
									"    pm.expect(modes).to.include.members([\"air\", \"express\", \"LCL\"]);\r",
									"});\r",
									"\r",
									"pm.test(\"Min price is less than max price for each mode\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode;\r",
									"\r",
									"    modes.forEach(m => {\r",
									"        pm.expect(m.price.min.moneyAmount.amount)\r",
									"            .to.be.below(m.price.max.moneyAmount.amount);\r",
									"    });\r",
									"});\r",
									"\r",
									"pm.test(\"Currency is USD for all modes\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode;\r",
									"\r",
									"    modes.forEach(m => {\r",
									"        pm.expect(m.price.min.moneyAmount.currency).to.eql(\"USD\");\r",
									"        pm.expect(m.price.max.moneyAmount.currency).to.eql(\"USD\");\r",
									"    });\r",
									"});\r",
									"\r",
									"pm.test(\"Transit time min is less than max\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode;\r",
									"\r",
									"    modes.forEach(m => {\r",
									"        pm.expect(m.transitTimes.min)\r",
									"            .to.be.below(m.transitTimes.max);\r",
									"    });\r",
									"});\r",
									"\r",
									"pm.test(\"Transit time unit is days\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode;\r",
									"\r",
									"    modes.forEach(m => {\r",
									"        pm.expect(m.transitTimes.unit).to.eql(\"days\");\r",
									"    });\r",
									"});\r",
									"\r",
									"pm.test(\"Number of quotes matches mode count\", function () {\r",
									"    const data = pm.response.json().response.estimatedFreightRates;\r",
									"    pm.expect(data.numQuotes).to.eql(data.mode.length);\r",
									"});"
								],
								"type": "text/javascript",
								"packages": {}
							}
						}
					],
					"request": {
						"method": "GET",
						"header": [],
						"url": {
							"raw": "{{URL}}loadtype=boxes&weight=200&width=50&length=50&height=50&origin=Shanghai,China&quantity=2&destination=NewYork,NY",
							"host": [
								"{{URL}}loadtype=boxes&weight=200&width=50&length=50&height=50&origin=Shanghai,China&quantity=2&destination=NewYork,NY"
							]
						}
					},
					"response": []
				},
				{
					"name": "Container Shipment - Orgn=CNSHA & Dst=USLGB",
					"event": [
						{
							"listen": "test",
							"script": {
								"exec": [
									"// Get XML response as text\r",
									"let xml = pm.response.text();\r",
									"\r",
									"// 1. Status code is 200\r",
									"pm.test(\"Status code is 200\", () => pm.response.to.have.status(200));\r",
									"\r",
									"// 2. estimatedFreightRates exists\r",
									"pm.test(\"estimatedFreightRates exists\", () => {\r",
									"    pm.expect(/<estimatedFreightRates/.test(xml)).to.be.true;\r",
									"});\r",
									"\r",
									"// 3. Number of quotes\r",
									"pm.test(\"Number of quotes is >= 0\", () => {\r",
									"    let match = xml.match(/<estimatedFreightRates[^>]*numQuotes=\"(\\d+)\"/);\r",
									"    pm.expect(match).to.not.be.null;\r",
									"    let numQuotes = parseInt(match[1]);\r",
									"    pm.expect(numQuotes).to.be.at.least(0);\r",
									"});\r",
									"\r",
									"// Only run detailed tests if there are quotes\r",
									"let numQuotes = parseInt(xml.match(/<estimatedFreightRates[^>]*numQuotes=\"(\\d+)\"/)[1]);\r",
									"if (numQuotes > 0) {\r",
									"\r",
									"    // Match all <mode> blocks\r",
									"    let modeMatches = [...xml.matchAll(/<mode[^>]*mode=\"([^\"]+)\"[\\s\\S]*?<\\/mode>/g)];\r",
									"    pm.test(\"At least one mode exists\", () => pm.expect(modeMatches.length).to.be.at.least(1));\r",
									"\r",
									"    modeMatches.forEach((modeMatch, index) => {\r",
									"        let modeBlock = modeMatch[0];\r",
									"        let modeName = modeMatch[1];\r",
									"\r",
									"        // Mode validation\r",
									"        pm.test(`Mode #${index + 1} is FCL`, () => pm.expect(modeName).to.eql(\"FCL\"));\r",
									"\r",
									"        // Prices\r",
									"        let minPrice = parseFloat(modeBlock.match(/<min>\\s*<moneyAmount[^>]*amount=\"([^\"]+)\"/)[1]);\r",
									"        let maxPrice = parseFloat(modeBlock.match(/<max>\\s*<moneyAmount[^>]*amount=\"([^\"]+)\"/)[1]);\r",
									"        pm.test(`Mode #${index + 1} prices exist`, () => {\r",
									"            pm.expect(minPrice).to.not.be.NaN;\r",
									"            pm.expect(maxPrice).to.not.be.NaN;\r",
									"        });\r",
									"        pm.test(`Mode #${index + 1} price validations`, () => {\r",
									"            pm.expect(minPrice).to.be.at.least(0);\r",
									"            pm.expect(maxPrice).to.be.at.least(minPrice);\r",
									"        });\r",
									"\r",
									"        // Currency\r",
									"        let minCurrency = modeBlock.match(/<min>\\s*<moneyAmount[^>]*currency=\"([^\"]+)\"/)[1];\r",
									"        let maxCurrency = modeBlock.match(/<max>\\s*<moneyAmount[^>]*currency=\"([^\"]+)\"/)[1];\r",
									"        pm.test(`Mode #${index + 1} currency is USD`, () => {\r",
									"            pm.expect(minCurrency).to.eql(\"USD\");\r",
									"            pm.expect(maxCurrency).to.eql(\"USD\");\r",
									"        });\r",
									"\r",
									"        // Transit times\r",
									"        let minDays = parseInt(modeBlock.match(/<transitTimes[^>]*>[\\s\\S]*<min>(\\d+)<\\/min>/)[1]);\r",
									"        let maxDays = parseInt(modeBlock.match(/<transitTimes[^>]*>[\\s\\S]*<max>(\\d+)<\\/max>/)[1]);\r",
									"        pm.test(`Mode #${index + 1} transit times exist`, () => {\r",
									"            pm.expect(minDays).to.not.be.NaN;\r",
									"            pm.expect(maxDays).to.not.be.NaN;\r",
									"        });\r",
									"        pm.test(`Mode #${index + 1} transit min <= max`, () => {\r",
									"            pm.expect(maxDays).to.be.at.least(minDays);\r",
									"        });\r",
									"    });\r",
									"} else {\r",
									"    pm.test(\"No quotes available, skipping detailed mode tests\", () => true);\r",
									"}"
								],
								"type": "text/javascript",
								"packages": {}
							}
						}
					],
					"request": {
						"method": "GET",
						"header": [],
						"url": {
							"raw": "{{URL}}format=xml&loadtype=container40&weight=15000&origin=CNSHA&quantity=2&destination=USLGB",
							"host": [
								"{{URL}}format=xml&loadtype=container40&weight=15000&origin=CNSHA&quantity=2&destination=USLGB"
							]
						}
					},
					"response": []
				},
				{
					"name": "Box Shipment - Orgn=US & Dst=CN",
					"event": [
						{
							"listen": "test",
							"script": {
								"exec": [
									"pm.test(\"Status code is 200\", function () {\r",
									"    pm.response.to.have.status(200);\r",
									"});\r",
									"\r",
									"/*pm.test(\"Response time is below 3000ms\", function () {\r",
									"    pm.expect(pm.response.responseTime).to.be.below(3000);\r",
									"});*/\r",
									"\r",
									"pm.test(\"Response object exists\", function () {\r",
									"    const jsonData = pm.response.json();\r",
									"    pm.expect(jsonData).to.have.property(\"response\");\r",
									"});\r",
									"\r",
									"pm.test(\"estimatedFreightRates is present\", function () {\r",
									"    const jsonData = pm.response.json();\r",
									"    pm.expect(jsonData.response).to.have.property(\"estimatedFreightRates\");\r",
									"});\r",
									"\r",
									"pm.test(\"Mode array is not empty\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode;\r",
									"    pm.expect(modes.length).to.be.greaterThan(0);\r",
									"});\r",
									"\r",
									"pm.test(\"Expected shipping modes are available\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode\r",
									"        .map(m => m.mode);\r",
									"\r",
									"    pm.expect(modes).to.include.members([\"air\", \"express\", \"LCL\"]);\r",
									"});\r",
									"\r",
									"pm.test(\"Currency is USD for all modes\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode;\r",
									"\r",
									"    modes.forEach(m => {\r",
									"        pm.expect(m.price.min.moneyAmount.currency).to.eql(\"USD\");\r",
									"        pm.expect(m.price.max.moneyAmount.currency).to.eql(\"USD\");\r",
									"    });\r",
									"});\r",
									"\r",
									"pm.test(\"Transit time min is less than max\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode;\r",
									"\r",
									"    modes.forEach(m => {\r",
									"        pm.expect(m.transitTimes.min)\r",
									"            .to.be.below(m.transitTimes.max);\r",
									"    });\r",
									"});\r",
									"\r",
									"pm.test(\"Transit time unit is days\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode;\r",
									"\r",
									"    modes.forEach(m => {\r",
									"        pm.expect(m.transitTimes.unit).to.eql(\"days\");\r",
									"    });\r",
									"});\r",
									"\r",
									"pm.test(\"Number of quotes matches mode count\", function () {\r",
									"    const data = pm.response.json().response.estimatedFreightRates;\r",
									"    pm.expect(data.numQuotes).to.eql(data.mode.length);\r",
									"});"
								],
								"type": "text/javascript"
							}
						}
					],
					"request": {
						"method": "GET",
						"header": [],
						"url": {
							"raw": "{{URL}}loadtype=boxes&weight=200&width=50&length=50&height=50&origin=NewYork,NY&quantity=2&destination=Shanghai,China",
							"host": [
								"{{URL}}loadtype=boxes&weight=200&width=50&length=50&height=50&origin=NewYork,NY&quantity=2&destination=Shanghai,China"
							]
						}
					},
					"response": []
				},
				{
					"name": "Container Shipment - Orgn=HKHKG & Dst=USNYC",
					"event": [
						{
							"listen": "test",
							"script": {
								"exec": [
									"// Get XML response as text\r",
									"let xml = pm.response.text();\r",
									"\r",
									"// 1. Status code is 200\r",
									"pm.test(\"Status code is 200\", () => pm.response.to.have.status(200));\r",
									"\r",
									"// 2. estimatedFreightRates exists\r",
									"pm.test(\"estimatedFreightRates exists\", () => {\r",
									"    pm.expect(/<estimatedFreightRates/.test(xml)).to.be.true;\r",
									"});\r",
									"\r",
									"// 3. Number of quotes\r",
									"pm.test(\"Number of quotes is >= 0\", () => {\r",
									"    let match = xml.match(/<estimatedFreightRates[^>]*numQuotes=\"(\\d+)\"/);\r",
									"    pm.expect(match).to.not.be.null;\r",
									"    let numQuotes = parseInt(match[1]);\r",
									"    pm.expect(numQuotes).to.be.at.least(0);\r",
									"});\r",
									"\r",
									"// Only run detailed tests if there are quotes\r",
									"let numQuotes = parseInt(xml.match(/<estimatedFreightRates[^>]*numQuotes=\"(\\d+)\"/)[1]);\r",
									"if (numQuotes > 0) {\r",
									"\r",
									"    // Match all <mode> blocks\r",
									"    let modeMatches = [...xml.matchAll(/<mode[^>]*mode=\"([^\"]+)\"[\\s\\S]*?<\\/mode>/g)];\r",
									"    pm.test(\"At least one mode exists\", () => pm.expect(modeMatches.length).to.be.at.least(1));\r",
									"\r",
									"    modeMatches.forEach((modeMatch, index) => {\r",
									"        let modeBlock = modeMatch[0];\r",
									"        let modeName = modeMatch[1];\r",
									"\r",
									"        // Mode validation\r",
									"        pm.test(`Mode #${index + 1} is FCL`, () => pm.expect(modeName).to.eql(\"FCL\"));\r",
									"\r",
									"        // Prices\r",
									"        let minPrice = parseFloat(modeBlock.match(/<min>\\s*<moneyAmount[^>]*amount=\"([^\"]+)\"/)[1]);\r",
									"        let maxPrice = parseFloat(modeBlock.match(/<max>\\s*<moneyAmount[^>]*amount=\"([^\"]+)\"/)[1]);\r",
									"        pm.test(`Mode #${index + 1} prices exist`, () => {\r",
									"            pm.expect(minPrice).to.not.be.NaN;\r",
									"            pm.expect(maxPrice).to.not.be.NaN;\r",
									"        });\r",
									"        pm.test(`Mode #${index + 1} price validations`, () => {\r",
									"            pm.expect(minPrice).to.be.at.least(0);\r",
									"            pm.expect(maxPrice).to.be.at.least(minPrice);\r",
									"        });\r",
									"\r",
									"        // Currency\r",
									"        let minCurrency = modeBlock.match(/<min>\\s*<moneyAmount[^>]*currency=\"([^\"]+)\"/)[1];\r",
									"        let maxCurrency = modeBlock.match(/<max>\\s*<moneyAmount[^>]*currency=\"([^\"]+)\"/)[1];\r",
									"        pm.test(`Mode #${index + 1} currency is USD`, () => {\r",
									"            pm.expect(minCurrency).to.eql(\"USD\");\r",
									"            pm.expect(maxCurrency).to.eql(\"USD\");\r",
									"        });\r",
									"\r",
									"        // Transit times\r",
									"        let minDays = parseInt(modeBlock.match(/<transitTimes[^>]*>[\\s\\S]*<min>(\\d+)<\\/min>/)[1]);\r",
									"        let maxDays = parseInt(modeBlock.match(/<transitTimes[^>]*>[\\s\\S]*<max>(\\d+)<\\/max>/)[1]);\r",
									"        pm.test(`Mode #${index + 1} transit times exist`, () => {\r",
									"            pm.expect(minDays).to.not.be.NaN;\r",
									"            pm.expect(maxDays).to.not.be.NaN;\r",
									"        });\r",
									"        pm.test(`Mode #${index + 1} transit min <= max`, () => {\r",
									"            pm.expect(maxDays).to.be.at.least(minDays);\r",
									"        });\r",
									"    });\r",
									"} else {\r",
									"    pm.test(\"No quotes available, skipping detailed mode tests\", () => true);\r",
									"}"
								],
								"type": "text/javascript",
								"packages": {}
							}
						}
					],
					"request": {
						"method": "GET",
						"header": [],
						"url": {
							"raw": "{{URL}}format=xml&loadtype=container40&weight=10000&origin=HKHKG&quantity=1&destination=USNYC",
							"host": [
								"{{URL}}format=xml&loadtype=container40&weight=10000&origin=HKHKG&quantity=1&destination=USNYC"
							]
						}
					},
					"response": []
				},
				{
					"name": "Box Shipment - Orgn=MY & Dst=AU",
					"event": [
						{
							"listen": "test",
							"script": {
								"exec": [
									"pm.test(\"Status code is 200\", function () {\r",
									"    pm.response.to.have.status(200);\r",
									"});\r",
									"\r",
									"/*pm.test(\"Response time is below 3000ms\", function () {\r",
									"    pm.expect(pm.response.responseTime).to.be.below(3000);\r",
									"});*/\r",
									"\r",
									"pm.test(\"Response object exists\", function () {\r",
									"    const jsonData = pm.response.json();\r",
									"    pm.expect(jsonData).to.have.property(\"response\");\r",
									"});\r",
									"\r",
									"pm.test(\"estimatedFreightRates is present\", function () {\r",
									"    const jsonData = pm.response.json();\r",
									"    pm.expect(jsonData.response).to.have.property(\"estimatedFreightRates\");\r",
									"});\r",
									"\r",
									"pm.test(\"Mode array is not empty\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode;\r",
									"    pm.expect(modes.length).to.be.greaterThan(0);\r",
									"});\r",
									"\r",
									"/* 🔧 MODIFIED: Make expected modes flexible */\r",
									"pm.test(\"Expected shipping modes are valid\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode\r",
									"        .map(m => m.mode);\r",
									"\r",
									"    const allowedModes = [\"air\", \"express\", \"LCL\"];\r",
									"    modes.forEach(mode => {\r",
									"        pm.expect(allowedModes).to.include(mode);\r",
									"    });\r",
									"});\r",
									"\r",
									"/* 🔧 MODIFIED: Allow min to be equal to max */\r",
									"pm.test(\"Min price is less than or equal to max price for each mode\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode;\r",
									"\r",
									"    modes.forEach(m => {\r",
									"        pm.expect(m.price.min.moneyAmount.amount)\r",
									"            .to.be.at.most(m.price.max.moneyAmount.amount);\r",
									"    });\r",
									"});\r",
									"\r",
									"pm.test(\"Currency is USD for all modes\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode;\r",
									"\r",
									"    modes.forEach(m => {\r",
									"        pm.expect(m.price.min.moneyAmount.currency).to.eql(\"USD\");\r",
									"        pm.expect(m.price.max.moneyAmount.currency).to.eql(\"USD\");\r",
									"    });\r",
									"});\r",
									"\r",
									"pm.test(\"Transit time min is less than max\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode;\r",
									"\r",
									"    modes.forEach(m => {\r",
									"        pm.expect(m.transitTimes.min)\r",
									"            .to.be.below(m.transitTimes.max);\r",
									"    });\r",
									"});\r",
									"\r",
									"pm.test(\"Transit time unit is days\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode;\r",
									"\r",
									"    modes.forEach(m => {\r",
									"        pm.expect(m.transitTimes.unit).to.eql(\"days\");\r",
									"    });\r",
									"});\r",
									"\r",
									"pm.test(\"Number of quotes matches mode count\", function () {\r",
									"    const data = pm.response.json().response.estimatedFreightRates;\r",
									"    pm.expect(data.numQuotes).to.eql(data.mode.length);\r",
									"});\r",
									""
								],
								"type": "text/javascript",
								"packages": {}
							}
						}
					],
					"request": {
						"method": "GET",
						"header": [],
						"url": {
							"raw": "{{URL}}loadtype=boxes&weight=500&width=50&length=70&height=50&origin=KualaLumpur,Malaysia&quantity=5&destination=Sydney,Australia",
							"host": [
								"{{URL}}loadtype=boxes&weight=500&width=50&length=70&height=50&origin=KualaLumpur,Malaysia&quantity=5&destination=Sydney,Australia"
							]
						}
					},
					"response": []
				},
				{
					"name": "Container Shipment - Orgn=HKHKG & Dst=USNYC1",
					"event": [
						{
							"listen": "test",
							"script": {
								"exec": [
									"// Get XML response as text\r",
									"let xml = pm.response.text();\r",
									"\r",
									"// 1. Status code is 200\r",
									"pm.test(\"Status code is 200\", () => pm.response.to.have.status(200));\r",
									"\r",
									"// 2. estimatedFreightRates exists\r",
									"pm.test(\"estimatedFreightRates exists\", () => {\r",
									"    pm.expect(/<estimatedFreightRates/.test(xml)).to.be.true;\r",
									"});\r",
									"\r",
									"// 3. Number of quotes\r",
									"pm.test(\"Number of quotes is >= 0\", () => {\r",
									"    let match = xml.match(/<estimatedFreightRates[^>]*numQuotes=\"(\\d+)\"/);\r",
									"    pm.expect(match).to.not.be.null;\r",
									"    let numQuotes = parseInt(match[1]);\r",
									"    pm.expect(numQuotes).to.be.at.least(0);\r",
									"});\r",
									"\r",
									"// Only run detailed tests if there are quotes\r",
									"let numQuotes = parseInt(xml.match(/<estimatedFreightRates[^>]*numQuotes=\"(\\d+)\"/)[1]);\r",
									"if (numQuotes > 0) {\r",
									"\r",
									"    // Match all <mode> blocks\r",
									"    let modeMatches = [...xml.matchAll(/<mode[^>]*mode=\"([^\"]+)\"[\\s\\S]*?<\\/mode>/g)];\r",
									"    pm.test(\"At least one mode exists\", () => pm.expect(modeMatches.length).to.be.at.least(1));\r",
									"\r",
									"    modeMatches.forEach((modeMatch, index) => {\r",
									"        let modeBlock = modeMatch[0];\r",
									"        let modeName = modeMatch[1];\r",
									"\r",
									"        // Mode validation\r",
									"        pm.test(`Mode #${index + 1} is FCL`, () => pm.expect(modeName).to.eql(\"FCL\"));\r",
									"\r",
									"        // Prices\r",
									"        let minPrice = parseFloat(modeBlock.match(/<min>\\s*<moneyAmount[^>]*amount=\"([^\"]+)\"/)[1]);\r",
									"        let maxPrice = parseFloat(modeBlock.match(/<max>\\s*<moneyAmount[^>]*amount=\"([^\"]+)\"/)[1]);\r",
									"        pm.test(`Mode #${index + 1} prices exist`, () => {\r",
									"            pm.expect(minPrice).to.not.be.NaN;\r",
									"            pm.expect(maxPrice).to.not.be.NaN;\r",
									"        });\r",
									"        pm.test(`Mode #${index + 1} price validations`, () => {\r",
									"            pm.expect(minPrice).to.be.at.least(0);\r",
									"            pm.expect(maxPrice).to.be.at.least(minPrice);\r",
									"        });\r",
									"\r",
									"        // Currency\r",
									"        let minCurrency = modeBlock.match(/<min>\\s*<moneyAmount[^>]*currency=\"([^\"]+)\"/)[1];\r",
									"        let maxCurrency = modeBlock.match(/<max>\\s*<moneyAmount[^>]*currency=\"([^\"]+)\"/)[1];\r",
									"        pm.test(`Mode #${index + 1} currency is USD`, () => {\r",
									"            pm.expect(minCurrency).to.eql(\"USD\");\r",
									"            pm.expect(maxCurrency).to.eql(\"USD\");\r",
									"        });\r",
									"\r",
									"        // Transit times\r",
									"        let minDays = parseInt(modeBlock.match(/<transitTimes[^>]*>[\\s\\S]*<min>(\\d+)<\\/min>/)[1]);\r",
									"        let maxDays = parseInt(modeBlock.match(/<transitTimes[^>]*>[\\s\\S]*<max>(\\d+)<\\/max>/)[1]);\r",
									"        pm.test(`Mode #${index + 1} transit times exist`, () => {\r",
									"            pm.expect(minDays).to.not.be.NaN;\r",
									"            pm.expect(maxDays).to.not.be.NaN;\r",
									"        });\r",
									"        pm.test(`Mode #${index + 1} transit min <= max`, () => {\r",
									"            pm.expect(maxDays).to.be.at.least(minDays);\r",
									"        });\r",
									"    });\r",
									"} else {\r",
									"    pm.test(\"No quotes available, skipping detailed mode tests\", () => true);\r",
									"}"
								],
								"type": "text/javascript"
							}
						}
					],
					"request": {
						"method": "GET",
						"header": [],
						"url": {
							"raw": "{{URL}}format=xml&loadtype=container40&weight=12360&origin=HKHKG&quantity=1&destination=USNYC",
							"host": [
								"{{URL}}format=xml&loadtype=container40&weight=12360&origin=HKHKG&quantity=1&destination=USNYC"
							]
						}
					},
					"response": []
				},
				{
					"name": "Box Shipment - Orgn=AU & Dst=MY",
					"event": [
						{
							"listen": "test",
							"script": {
								"exec": [
									"pm.test(\"Status code is 200\", function () {\r",
									"    pm.response.to.have.status(200);\r",
									"});\r",
									"\r",
									"/*pm.test(\"Response time is below 3000ms\", function () {\r",
									"    pm.expect(pm.response.responseTime).to.be.below(3000);\r",
									"});*/\r",
									"\r",
									"pm.test(\"Response object exists\", function () {\r",
									"    const jsonData = pm.response.json();\r",
									"    pm.expect(jsonData).to.have.property(\"response\");\r",
									"});\r",
									"\r",
									"pm.test(\"estimatedFreightRates is present\", function () {\r",
									"    const jsonData = pm.response.json();\r",
									"    pm.expect(jsonData.response).to.have.property(\"estimatedFreightRates\");\r",
									"});"
								],
								"type": "text/javascript"
							}
						}
					],
					"request": {
						"method": "GET",
						"header": [],
						"url": {
							"raw": "{{URL}}loadtype=boxes&weight=500&width=50&length=70&height=50&origin=Sydney,Australia&quantity=5&destination=KualaLumpur,Malaysia",
							"host": [
								"{{URL}}loadtype=boxes&weight=500&width=50&length=70&height=50&origin=Sydney,Australia&quantity=5&destination=KualaLumpur,Malaysia"
							]
						}
					},
					"response": []
				},
				{
					"name": "Container Shipment - Orgn=HKHKG & Dst=USNYC2",
					"event": [
						{
							"listen": "test",
							"script": {
								"exec": [
									"// Get XML response as text\r",
									"let xml = pm.response.text();\r",
									"\r",
									"// 1. Status code is 200\r",
									"pm.test(\"Status code is 200\", () => pm.response.to.have.status(200));\r",
									"\r",
									"// 2. estimatedFreightRates exists\r",
									"pm.test(\"estimatedFreightRates exists\", () => {\r",
									"    pm.expect(/<estimatedFreightRates/.test(xml)).to.be.true;\r",
									"});\r",
									"\r",
									"// 3. Number of quotes\r",
									"pm.test(\"Number of quotes is >= 0\", () => {\r",
									"    let match = xml.match(/<estimatedFreightRates[^>]*numQuotes=\"(\\d+)\"/);\r",
									"    pm.expect(match).to.not.be.null;\r",
									"    let numQuotes = parseInt(match[1]);\r",
									"    pm.expect(numQuotes).to.be.at.least(0);\r",
									"});\r",
									"\r",
									"// Only run detailed tests if there are quotes\r",
									"let numQuotes = parseInt(xml.match(/<estimatedFreightRates[^>]*numQuotes=\"(\\d+)\"/)[1]);\r",
									"if (numQuotes > 0) {\r",
									"\r",
									"    // Match all <mode> blocks\r",
									"    let modeMatches = [...xml.matchAll(/<mode[^>]*mode=\"([^\"]+)\"[\\s\\S]*?<\\/mode>/g)];\r",
									"    pm.test(\"At least one mode exists\", () => pm.expect(modeMatches.length).to.be.at.least(1));\r",
									"\r",
									"    modeMatches.forEach((modeMatch, index) => {\r",
									"        let modeBlock = modeMatch[0];\r",
									"        let modeName = modeMatch[1];\r",
									"\r",
									"        // Mode validation\r",
									"        pm.test(`Mode #${index + 1} is FCL`, () => pm.expect(modeName).to.eql(\"FCL\"));\r",
									"\r",
									"        // Prices\r",
									"        let minPrice = parseFloat(modeBlock.match(/<min>\\s*<moneyAmount[^>]*amount=\"([^\"]+)\"/)[1]);\r",
									"        let maxPrice = parseFloat(modeBlock.match(/<max>\\s*<moneyAmount[^>]*amount=\"([^\"]+)\"/)[1]);\r",
									"        pm.test(`Mode #${index + 1} prices exist`, () => {\r",
									"            pm.expect(minPrice).to.not.be.NaN;\r",
									"            pm.expect(maxPrice).to.not.be.NaN;\r",
									"        });\r",
									"        pm.test(`Mode #${index + 1} price validations`, () => {\r",
									"            pm.expect(minPrice).to.be.at.least(0);\r",
									"            pm.expect(maxPrice).to.be.at.least(minPrice);\r",
									"        });\r",
									"\r",
									"        // Currency\r",
									"        let minCurrency = modeBlock.match(/<min>\\s*<moneyAmount[^>]*currency=\"([^\"]+)\"/)[1];\r",
									"        let maxCurrency = modeBlock.match(/<max>\\s*<moneyAmount[^>]*currency=\"([^\"]+)\"/)[1];\r",
									"        pm.test(`Mode #${index + 1} currency is USD`, () => {\r",
									"            pm.expect(minCurrency).to.eql(\"USD\");\r",
									"            pm.expect(maxCurrency).to.eql(\"USD\");\r",
									"        });\r",
									"\r",
									"        // Transit times\r",
									"        let minDays = parseInt(modeBlock.match(/<transitTimes[^>]*>[\\s\\S]*<min>(\\d+)<\\/min>/)[1]);\r",
									"        let maxDays = parseInt(modeBlock.match(/<transitTimes[^>]*>[\\s\\S]*<max>(\\d+)<\\/max>/)[1]);\r",
									"        pm.test(`Mode #${index + 1} transit times exist`, () => {\r",
									"            pm.expect(minDays).to.not.be.NaN;\r",
									"            pm.expect(maxDays).to.not.be.NaN;\r",
									"        });\r",
									"        pm.test(`Mode #${index + 1} transit min <= max`, () => {\r",
									"            pm.expect(maxDays).to.be.at.least(minDays);\r",
									"        });\r",
									"    });\r",
									"} else {\r",
									"    pm.test(\"No quotes available, skipping detailed mode tests\", () => true);\r",
									"}"
								],
								"type": "text/javascript"
							}
						}
					],
					"request": {
						"method": "GET",
						"header": [],
						"url": {
							"raw": "{{URL}}format=xml&loadtype=container40&weight=12834&origin=HKHKG&quantity=1&destination=USNYC",
							"host": [
								"{{URL}}format=xml&loadtype=container40&weight=12834&origin=HKHKG&quantity=1&destination=USNYC"
							]
						}
					},
					"response": []
				},
				{
					"name": "Box Shipment - Orgn=DE & Dst=CA",
					"event": [
						{
							"listen": "test",
							"script": {
								"exec": [
									"pm.test(\"Status code is 200\", function () {\r",
									"    pm.response.to.have.status(200);\r",
									"});\r",
									"\r",
									"/*pm.test(\"Response time is below 3000ms\", function () {\r",
									"    pm.expect(pm.response.responseTime).to.be.below(3000);\r",
									"});*/\r",
									"\r",
									"pm.test(\"Response object exists\", function () {\r",
									"    const jsonData = pm.response.json();\r",
									"    pm.expect(jsonData).to.have.property(\"response\");\r",
									"});\r",
									"\r",
									"pm.test(\"estimatedFreightRates is present\", function () {\r",
									"    const jsonData = pm.response.json();\r",
									"    pm.expect(jsonData.response).to.have.property(\"estimatedFreightRates\");\r",
									"});\r",
									"\r",
									"pm.test(\"Mode array is not empty\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode;\r",
									"    pm.expect(modes.length).to.be.greaterThan(0);\r",
									"});\r",
									"\r",
									"pm.test(\"Expected shipping modes are valid\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode\r",
									"        .map(m => m.mode);\r",
									"\r",
									"    const allowedModes = [\"air\", \"express\", \"LCL\"];\r",
									"    modes.forEach(mode => {\r",
									"        pm.expect(allowedModes).to.include(mode);\r",
									"    });\r",
									"});\r",
									"\r",
									"pm.test(\"Min price is less than or equal to max price for each mode\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode;\r",
									"\r",
									"    modes.forEach(m => {\r",
									"        pm.expect(m.price.min.moneyAmount.amount)\r",
									"            .to.be.at.most(m.price.max.moneyAmount.amount);\r",
									"    });\r",
									"});\r",
									"\r",
									"pm.test(\"Currency is USD for all modes\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode;\r",
									"\r",
									"    modes.forEach(m => {\r",
									"        pm.expect(m.price.min.moneyAmount.currency).to.eql(\"USD\");\r",
									"        pm.expect(m.price.max.moneyAmount.currency).to.eql(\"USD\");\r",
									"    });\r",
									"});\r",
									"\r",
									"pm.test(\"Transit time min is less than or equal to max\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode;\r",
									"\r",
									"    modes.forEach(m => {\r",
									"        pm.expect(m.transitTimes.min)\r",
									"            .to.be.at.most(m.transitTimes.max);\r",
									"    });\r",
									"});\r",
									"\r",
									"pm.test(\"Transit time unit is days\", function () {\r",
									"    const modes = pm.response.json().response.estimatedFreightRates.mode;\r",
									"\r",
									"    modes.forEach(m => {\r",
									"        pm.expect(m.transitTimes.unit).to.eql(\"days\");\r",
									"    });\r",
									"});\r",
									"\r",
									"pm.test(\"Number of quotes matches mode count\", function () {\r",
									"    const data = pm.response.json().response.estimatedFreightRates;\r",
									"    pm.expect(data.numQuotes).to.eql(data.mode.length);\r",
									"});"
								],
								"type": "text/javascript",
								"packages": {}
							}
						}
					],
					"request": {
						"method": "GET",
						"header": [],
						"url": {
							"raw": "{{URL}}loadtype=boxes&weight=300&width=55&length=60&height=40&origin=Hamburg,Germany&quantity=7&destination=Toronto,Canada",
							"host": [
								"{{URL}}loadtype=boxes&weight=300&width=55&length=60&height=40&origin=Hamburg,Germany&quantity=7&destination=Toronto,Canada"
							]
						}
					},
					"response": []
				},
				{
					"name": "Container Shipment - Orgn=CNSHA & Dst=USLGB2",
					"event": [
						{
							"listen": "test",
							"script": {
								"exec": [
									"// Get XML response as text\r",
									"let xml = pm.response.text();\r",
									"\r",
									"// 1. Status code is 200\r",
									"pm.test(\"Status code is 200\", () => pm.response.to.have.status(200));\r",
									"\r",
									"// 2. estimatedFreightRates exists\r",
									"pm.test(\"estimatedFreightRates exists\", () => {\r",
									"    pm.expect(/<estimatedFreightRates/.test(xml)).to.be.true;\r",
									"});\r",
									"\r",
									"// 3. Number of quotes\r",
									"pm.test(\"Number of quotes is >= 0\", () => {\r",
									"    let match = xml.match(/<estimatedFreightRates[^>]*numQuotes=\"(\\d+)\"/);\r",
									"    pm.expect(match).to.not.be.null;\r",
									"    let numQuotes = parseInt(match[1]);\r",
									"    pm.expect(numQuotes).to.be.at.least(0);\r",
									"});\r",
									"\r",
									"// Only run detailed tests if there are quotes\r",
									"let numQuotes = parseInt(xml.match(/<estimatedFreightRates[^>]*numQuotes=\"(\\d+)\"/)[1]);\r",
									"if (numQuotes > 0) {\r",
									"\r",
									"    // Match all <mode> blocks\r",
									"    let modeMatches = [...xml.matchAll(/<mode[^>]*mode=\"([^\"]+)\"[\\s\\S]*?<\\/mode>/g)];\r",
									"    pm.test(\"At least one mode exists\", () => pm.expect(modeMatches.length).to.be.at.least(1));\r",
									"\r",
									"    modeMatches.forEach((modeMatch, index) => {\r",
									"        let modeBlock = modeMatch[0];\r",
									"        let modeName = modeMatch[1];\r",
									"\r",
									"        // Mode validation\r",
									"        pm.test(`Mode #${index + 1} is FCL`, () => pm.expect(modeName).to.eql(\"FCL\"));\r",
									"\r",
									"        // Prices\r",
									"        let minPrice = parseFloat(modeBlock.match(/<min>\\s*<moneyAmount[^>]*amount=\"([^\"]+)\"/)[1]);\r",
									"        let maxPrice = parseFloat(modeBlock.match(/<max>\\s*<moneyAmount[^>]*amount=\"([^\"]+)\"/)[1]);\r",
									"        pm.test(`Mode #${index + 1} prices exist`, () => {\r",
									"            pm.expect(minPrice).to.not.be.NaN;\r",
									"            pm.expect(maxPrice).to.not.be.NaN;\r",
									"        });\r",
									"        pm.test(`Mode #${index + 1} price validations`, () => {\r",
									"            pm.expect(minPrice).to.be.at.least(0);\r",
									"            pm.expect(maxPrice).to.be.at.least(minPrice);\r",
									"        });\r",
									"\r",
									"        // Currency\r",
									"        let minCurrency = modeBlock.match(/<min>\\s*<moneyAmount[^>]*currency=\"([^\"]+)\"/)[1];\r",
									"        let maxCurrency = modeBlock.match(/<max>\\s*<moneyAmount[^>]*currency=\"([^\"]+)\"/)[1];\r",
									"        pm.test(`Mode #${index + 1} currency is USD`, () => {\r",
									"            pm.expect(minCurrency).to.eql(\"USD\");\r",
									"            pm.expect(maxCurrency).to.eql(\"USD\");\r",
									"        });\r",
									"\r",
									"        // Transit times\r",
									"        let minDays = parseInt(modeBlock.match(/<transitTimes[^>]*>[\\s\\S]*<min>(\\d+)<\\/min>/)[1]);\r",
									"        let maxDays = parseInt(modeBlock.match(/<transitTimes[^>]*>[\\s\\S]*<max>(\\d+)<\\/max>/)[1]);\r",
									"        pm.test(`Mode #${index + 1} transit times exist`, () => {\r",
									"            pm.expect(minDays).to.not.be.NaN;\r",
									"            pm.expect(maxDays).to.not.be.NaN;\r",
									"        });\r",
									"        pm.test(`Mode #${index + 1} transit min <= max`, () => {\r",
									"            pm.expect(maxDays).to.be.at.least(minDays);\r",
									"        });\r",
									"    });\r",
									"} else {\r",
									"    pm.test(\"No quotes available, skipping detailed mode tests\", () => true);\r",
									"}"
								],
								"type": "text/javascript"
							}
						}
					],
					"request": {
						"method": "GET",
						"header": [],
						"url": {
							"raw": "{{URL}}format=xml&loadtype=container40&weight=17700&origin=CNSHA&quantity=3&destination=USLGB",
							"host": [
								"{{URL}}format=xml&loadtype=container40&weight=17700&origin=CNSHA&quantity=3&destination=USLGB"
							]
						}
					},
					"response": []
				}
			]
		}
	]
}